# Interactive lesson widgets — pilot for review

Branch `lesson-widgets` (worktree, nothing merged, nothing deployed, no
Supabase content changed). Three widgets in three different subjects.

## Look at them

Review server is running. If it has stopped:
`python scripts/_dev_rewrite_server.py 8919` from this worktree.

| What | URL |
|---|---|
| All three side by side | http://127.0.0.1:8919/widgets-test.html |
| Physics — in its lesson | http://127.0.0.1:8919/lesson/science-aqa/physics-paper-2/6 |
| Geography — in its lesson | http://127.0.0.1:8919/lesson/geography-aqa/paper-1/19 |
| Computer Science — in its lesson | http://127.0.0.1:8919/lesson/computer-science/computational-thinking/4 |

## The three

**1. Waves (Science AQA, Physics Paper 2 L6)** — sits after the wave
equation, before refraction. Wave speed is FIXED at 6 m/s (a property of
the medium), so dragging frequency visibly shortens the wavelength while
v = f×λ stays balanced — the point students most often miss. Amplitude is
separate and changes energy, not speed. A toggle switches to a
longitudinal view with compressions and rarefactions.

**2. Storm hydrograph (Geography AQA, Paper 1 L19)** — sits after the
"flashy hydrograph" key fact, before the factors sections it lets you
test. Same storm every time; three sliders (relief, geology, land use)
change the basin. Lag time and peak discharge update live, the limbs are
labelled, and the caption names the outcome in the lesson's own words
("flashy hydrograph… high flood risk").

**3. Sorting algorithms (Computer Science OCR, Computational Thinking L4)**
— sits after the three algorithms are taught, before "Comparing the
algorithms", so students can race them. Step through every comparison and
swap, or press Play. Insertion sort shows the lifted value held above a
dashed gap, exactly as exam diagrams draw it. Comparison and move counters
let students see *why* merge sort wins.

## How they're built (the important bit)

Hand-built components, injected by URL from a map in
`js/lesson-widgets.js` — **not** generated per lesson. This is the
annotated-music-player model: build once, verify once, reuse with data.
The alternative (a model writing a fresh interactive per lesson) is the
Gemini-diagram trap: unverifiable at scale, and a wrong *interactive*
teaches wrong physics rather than merely looking wrong.

Supabase content is untouched, so nothing to un-pick if you say no.

## What I verified

- **Physics numerically**: v = f×λ holds at exactly 6 m/s across the
  frequency range, and T×f = 1. Not eyeballed — measured.
- **Sorting against the real algorithms**: the animation replays a script
  generated by *running* the algorithm, so it cannot drift. All three end
  sorted, and no value is ever lost or duplicated at any intermediate step.
- **Two bugs the audit caught and I fixed**: insertion sort under-reported
  comparisons (3 recorded vs 15 actually performed — bad, since OCR
  students compare algorithms by that number), and it showed a duplicated
  bar where the exam diagram shows a gap.
- **Placement** in all three lessons, **no horizontal overflow at 360px**,
  controls stack cleanly on a phone.
- Canvas-only state is mirrored to a screen-reader live region.
  `prefers-reduced-motion` starts the wave paused.

## Site-wide audit — what to build next

`scripts/_widget_audit.md` (full list) ranks nine archetypes by how many
of the 4,063 live article lessons **one component** would serve:

| Widget | Lessons | Shape of the win |
|---|---|---|
| Sort-into-categories drill | 265 | Widest — 73 subjects. D&T 35, science 27, geography 17 |
| Interactive timeline | 214 | Most concentrated — **169 are history**, all four boards |
| Process stepper | 174 | Science 39, separate sciences 29, CS 20, geography 19 |
| Equation explorer | 172 | **146 are science** — one component, per-lesson equation data |
| Labelled structure explorer | 91 | Biology and geography landforms |
| Graph shape explorer | 80 | The pilot hydrograph generalised |
| System model | 59 | Ecosystems, market equilibrium, homeostasis |
| Map / spatial explorer | 39 | Geography + geology |
| Build-it-right builder | 31 | Music — chords and cadences |

**My recommended build order**, on coverage-per-unit-of-effort:

1. **Equation explorer** — highest leverage on the site. One component;
   each lesson supplies only an equation, variable names, units and
   ranges, so 172 lessons come from roughly one build plus data entry.
   It also directly serves the subject students find hardest.
2. **Interactive timeline** — history has *no* interactive content at all
   today, and 169 lessons want the same component. Biggest single-subject
   transformation available.
3. **Process stepper** — the sorting widget generalised (it is already a
   stepper); moderate extra work, 174 lessons.
4. **Sort-into-categories** — widest reach but the shallowest teaching
   value per lesson; worth doing once the three above land.

Caveats worth knowing: a lesson can match several archetypes (1,125
matches over 4,063 lessons); school copies of generic lessons appear
separately, so a few counts are inflated by Unity duplicates; and the
scan finds lessons whose *text* signals the archetype — a human still
picks the exemplars, which is what Phase 2 triage is for.

## Open questions for you

1. **Are these worth building more of?** My shortlist next: circuits
   (V = IR), motion graphs (distance–time vs velocity–time), rates of
   reaction, population pyramids, binary/denary converter.
2. **Placement** — mid-lesson (as now) or collected at the end as a "play
   with it" section?
3. **Free tier or Unity-only?** These carry no per-lesson generation cost,
   so unlike diagrams there is no reason to hold them back — but it is a
   differentiator worth a decision.
4. Should a widget interaction count toward lesson completion / the
   dashboard, or stay unmeasured?
