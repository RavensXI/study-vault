# Fact-check — computer-science-edexcel-python-pls

Checked 2 lessons. HIGH=0 MED=6 LOW=6

- **medium** `programming-python-L07` [content_html]: Unevidenced claim about exam frequency. Neither the 1CP2 specification extract nor the PLS v6 states anything about how often a particular formatting pattern is set in Paper 2 tasks; the PLS simply gives this as an illus → Study the code and its output carefully, because this example from the PLS combines all three options — a string, an integer and a real number — in one format s
- **medium** `programming-python-L07` [content_html]: Only wrong in one direction. Verified in Python 3: "{:d} {:d}".format(1) raises IndexError, but supplying more values than placeholders is silently ignored — "{}".format(1, 2, 3) returns '1' with no error. As written the → Giving <code>.format()</code> fewer values than there are placeholders raises an error (an IndexError); note that surplus values are simply ignored, so a mismat
- **medium** `programming-python-L07` [flashcard_questions]: The card is factually wrong as stated: an IndexError is raised only when there are too few values. Verified in Python 3 that extra values are ignored without error, so a 'mismatch' does not always produce an error. → {"q": "What error occurs if .format() is given fewer values than there are placeholders?", "a": "Python raises an IndexError; surplus values, by contrast, are i
- **low** `programming-python-L07` [content_html]: Imprecise. The PLS v6 built-in subprograms table describes round as returning a rounded value ('Rounds <x> to the number of <n> digits after the decimal'); it does not modify the variable passed to it. round(total, 2) le → <code>round (&lt;x&gt;, &lt;n&gt;)</code> returns a new rounded number, so if you store that result, later maths uses the rounded figure.
- **low** `programming-python-L07` [flashcard_questions]: Same imprecision as the body text: round() returns a rounded value rather than altering the variable given to it (PLS v6, built-in subprograms table). → {"q": "What is the key difference between round(x, 2) and a format placeholder like {:.2f}?", "a": "round() returns a rounded value you can store and reuse; the
- **medium** `programming-python-L08` [exam_tip_html]: Contradicts the PLS v6 Introduction (the source text), which states that Pearson will not go beyond the PLS when setting tasks but that a student successfully using constructs not included in the document will still be a → Stick to the subprogram names and argument order shown in the PLS: everything Pearson sets can be solved with PLS constructs, and the PLS is supplied as an inse
- **medium** `programming-python-L08` [glossary_terms]: Wrong definition of a constant. The PLS states only that 'Constants are conventionally named in all uppercase characters', and specification point 6.3.2 treats constants as ordinary program values; a constant is any name → constant — A named value that is set once and does not change while the program runs; constants are conventionally named in all uppercase characters. Library mo
- **medium** `programming-python-L08` [glossary_terms]: This is the definition of an argument, not a parameter, and it duplicates the lesson's own (correct) 'arguments' entry, so the two glossary terms contradict each other. A parameter is the named variable in the subprogram → parameter — A variable named in a subprogram's definition, such as <code>&lt;paramA&gt;</code> in <code>def calculate (paramA, paramB):</code>, which receives t
- **low** `programming-python-L08` [content_html]: Terminology inconsistent with the lesson's own (correct) definition of 'arguments' earlier in the same lesson: the value supplied in the call, e.g. the 1 in <code>time.sleep(1)</code>, is an argument; <code>&lt;sec&gt;</ → <code>time.sleep (&lt;sec&gt;)</code> pauses the program for the number of seconds given as its <dfn class="term" data-def="Values placed inside the brackets of
- **low** `programming-python-L08` [content_html]: The stated order contradicts the lesson's own worked example, which correctly sets <code>t.fillcolor("blue")</code> before <code>t.begin_fill()</code>. The PLS describes <code>fillcolor</code> as setting the colour used  → Filling: set <code>&lt;turtle&gt;.fillcolor (&lt;colour&gt;)</code>, call <code>&lt;turtle&gt;.begin_fill ()</code> just before drawing the shape, then call <co
- **low** `programming-python-L08` [content_html]: Overstated: the PLS says the turtle window closes as soon as the program completes only 'in some development environments', and it gives two ways to keep it open — <code>turtle.done ()</code> or a final <code>input ()</c → Leaving out <code>turtle.done ()</code> at the end of a drawing program, so that in some development environments the window closes as soon as the program finis
- **low** `programming-python-L08` [knowledge_checks]: Stray space between the object name and the dot; the PLS writes this as <code>&lt;turtle&gt;.left (&lt;degrees&gt;)</code>, and <code>turtle .left(degrees)</code> as printed is not the form students should copy. → "turtle.left(degrees)"