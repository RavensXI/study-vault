[
 {
  "lesson": 1,
  "field": "content_html",
  "find": "The DI box performs <strong>level matching</strong> — it converts the instrument’s high-impedance, unbalanced signal into a low-impedance, balanced signal.",
  "replace": "The DI box converts the instrument’s high-impedance, unbalanced signal into a low-impedance, balanced signal. It also carries out <strong>level matching</strong> — it drops the strong instrument signal down to microphone level for the desk input.",
  "severity": "MEDIUM",
  "note": "F1: level matching is the drop to mic level, not the impedance and balance conversion."
 },
 {
  "lesson": 1,
  "field": "practice_questions",
  "path": "[2].marks",
  "expect": "Award 1 mark per valid point (max 3): A DI box converts an instrument's high-impedance unbalanced signal to a low-impedance balanced signal (1). This is called level matching (1). It reduces noise on long cable runs (1). It makes the signal compatible with a mixing desk or audio interface (1).",
  "value": "Award 1 mark per valid point (max 3): A DI box converts an instrument's high-impedance unbalanced signal to a low-impedance balanced signal (1). It also drops the signal to microphone level, which is level matching (1). It reduces noise on long cable runs (1). It makes the signal compatible with a mixing desk or audio interface (1).",
  "severity": "MEDIUM",
  "note": "F2: the answer key repeated the F1 mislabel."
 },
 {
  "lesson": 1,
  "field": "knowledge_checks",
  "path": "[0].options[3]",
  "expect": "Bidirectional",
  "value": "Hypercardioid",
  "severity": "MEDIUM",
  "note": "F3: 'Bidirectional' duplicated option 1 'Figure-of-eight'; the key index 2 (Cardioid) is unchanged."
 },
 {
  "lesson": 3,
  "field": "content_html",
  "find": "The <strong>ratio</strong> controls how much reduction is applied (e.g. 4:1 means for every 4 dB over the threshold, only 1 dB passes through).",
  "replace": "The <strong>ratio</strong> controls how much reduction is applied (e.g. with 4:1, a signal 4 dB above the threshold comes out only 1 dB above it).",
  "severity": "MEDIUM",
  "note": "F4: 4:1 cuts the overshoot to a quarter; it does not cut the output to 1 dB."
 },
 {
  "lesson": 3,
  "field": "flashcard_questions",
  "path": "[9].a",
  "expect": "For every 4 dB above the threshold, only 1 dB of signal passes through.",
  "value": "A signal 4 dB above the threshold comes out only 1 dB above it.",
  "severity": "MEDIUM",
  "note": "F5: the same ratio correction as F4."
 },
 {
  "lesson": 3,
  "field": "content_html",
  "find": "<strong>near-field speakers</strong> (smaller monitors) that simulate a typical listening environment.",
  "replace": "<strong>near-field speakers</strong> (smaller monitors placed close to the engineer, so room reflections affect the sound less).",
  "severity": "MEDIUM",
  "note": "F6: near-field monitors are about position and direct sound, not imitating a home system."
 }
]
