{
  "key": "probability-statistics-L05",
  "figures_added": [
    {
      "tier": "silver",
      "index": 1,
      "kind": "svg",
      "what": "Histogram with proportional bar widths for classes 0-10 (fd 2), 10-20 (fd 5), 20-50 (fd 3). The 20-50 bar is drawn 3x wider than the width-10 bars, making the width trap (its width is 30, not 10) visible. Problem previously described a fully-specified histogram in text with no figure."
    },
    {
      "tier": "teach.bronze",
      "index": null,
      "kind": "svg",
      "what": "Cumulative frequency curve (n=60) passing exactly through the three read-offs the walk quotes: mark 40 at CF 15, mark 52 at CF 30 (median), mark 64 at CF 45. Monotonic curve with read-off dots. Display text explicitly claimed a CF graph but showed none."
    },
    {
      "tier": "teach.silver",
      "index": null,
      "kind": "svg",
      "what": "Histogram with proportional widths for waiting-time classes 0-10 (fd 3), 10-30 (fd 4), 30-40 (fd 2). The 10-30 bar renders width-20, the tier's whole point. Text claimed a histogram of three bars."
    },
    {
      "tier": "teach.gold",
      "index": null,
      "kind": "svg",
      "what": "Single-bar histogram, house prices 200-300 (thousand pounds) at fd 0.6, with a dashed divider at 250 marking the 250-300 slice the walk estimates. Text claimed a histogram."
    }
  ],
  "opener_touched": false,
  "notes": "Diagram pass only: no question text, solutions, guided_steps, hints, or misconceptions changed. All SVGs generated programmatically from each problem's own numbers (viewBox 0 0 260 175, role=img, aria-label, currentColor strokes/text, soft fills at fill-opacity 0.3, Inter labels, ~2KB each, no external refs). Non-scale figures (silver[1] histogram, teach.gold histogram) carry a 'Diagram not drawn accurately' caption. Opener left untouched: it deliberately shows running totals with no graph ('no graph needed'). Existing Chart.js figures on gold[0-1], bronze[0-4], silver[0,2,3] preserved unchanged. Problems left without figures are genuinely under-specified for a faithful drawing (only summary stats like median/IQR given, e.g. gold[2-4], bronze[5-7], silver[4-6]) or pure single-value formula drills, so drawing would risk mislabelling. Validator PASS on the shipped file; live row PATCH 204 and round-trip confirms all four SVGs present."
}
