# maths-aqa / algebra / L10 - Simultaneous Equations (Quadratic)

## bronze[0] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = x\) and \(y = x^2\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(x = x^2\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-1, label:'x']
   - ask: and the constant becomes  [box=0, NO label]
   - intro: So \(x^2 - x = 0\). Take out a common factor of x: \(x(x - 1) = 0\).
   - ask: First root, x =  [box=0, NO label]
   - ask: Second root, x =  [box=1, NO label]
   - intro: Now each x gets its y from the line \(y = x\).
   - ask: At x = 0: y =  [box=0, NO label]
   - ask: At x = 1: y =  [box=1, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Check in the curve: 0² =  [box=0, NO label]

## bronze[1] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 3\) and \(y = x^2 - 1\). Give the two x-values.
   - intro: One equation is just \(y = 3\), so the curve must equal 3: \(x^2 - 1 = 3\).
   - ask: Rearrange to x² =  [box=4, NO label]
   - intro: \(x^2 = 4\), so x is a square root of 4, and there are TWO: \(+2\) and \(−2\).
   - ask: First root (positive), x =  [box=2, NO label]
   - ask: Second root (negative), x =  [box=-2, NO label]
   - ask: Check the curve at x = 2: 2² − 1 =  [box=3, NO label]

## bronze[2] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = x + 2\) and \(y = x^2\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(x + 2 = x^2\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-1, label:'x']
   - ask: and the constant becomes  [box=-2, NO label]
   - intro: So \(x^2 - x - 2 = 0\). Two numbers multiply to \(-2\) and add to \(-1\): they are \(-2\) and \(+1\), giving \((x - 2)(x + 1) = 0\).
   - ask: First root, x =  [box=2, NO label]
   - ask: Second root, x =  [box=-1, NO label]
   - intro: Now each x gets its y from the line \(y = x + 2\).
   - ask: At x = 2: y =  [box=4, NO label]
   - ask: At x = −1: y =  [box=1, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Check in the curve: 2² =  [box=4, NO label]

## bronze[3] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 4x\) and \(y = x^2 + 3\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(4x = x^2 + 3\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-4, label:'x']
   - ask: and the constant becomes  [box=3, NO label]
   - intro: So \(x^2 - 4x + 3 = 0\). Two numbers multiply to 3 and add to \(-4\): they are \(-1\) and \(-3\), giving \((x - 1)(x - 3) = 0\).
   - ask: First root, x =  [box=1, NO label]
   - ask: Second root, x =  [box=3, NO label]
   - intro: Now each x gets its y from the line \(y = 4x\).
   - ask: At x = 1: y =  [box=4, NO label]
   - ask: At x = 3: y =  [box=12, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Check in the curve: 1² + 3 =  [box=4, NO label]

## bronze[4] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 2x\) and \(y = x^2 - 3\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(2x = x^2 - 3\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-2, label:'x']
   - ask: and the constant becomes  [box=-3, NO label]
   - intro: So \(x^2 - 2x - 3 = 0\). Two numbers multiply to \(-3\) and add to \(-2\): they are \(-3\) and \(+1\), giving \((x - 3)(x + 1) = 0\).
   - ask: First root, x =  [box=3, NO label]
   - ask: Second root, x =  [box=-1, NO label]
   - intro: Now each x gets its y from the line \(y = 2x\).
   - ask: At x = 3: y =  [box=6, NO label]
   - ask: At x = −1: y =  [box=-2, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Check in the curve: 3² − 3 =  [box=6, NO label]

## bronze[5] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = x + 6\) and \(y = x^2 + 2x\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(x + 6 = x^2 + 2x\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=1, label:'x']
   - ask: and the constant becomes  [box=-6, NO label]
   - intro: So \(x^2 + x - 6 = 0\). Two numbers multiply to \(-6\) and add to \(+1\): they are \(+3\) and \(-2\), giving \((x + 3)(x - 2) = 0\).
   - ask: First root, x =  [box=-3, NO label]
   - ask: Second root, x =  [box=2, NO label]
   - intro: Now each x gets its y from the line \(y = x + 6\).
   - ask: At x = −3: y =  [box=3, NO label]
   - ask: At x = 2: y =  [box=8, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Check in the curve: (−3)² + 2×(−3) =  [box=3, NO label]

## bronze[6] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 7\) and \(y = x^2 - 2\). Give the two x-values.
   - intro: One equation is just \(y = 7\), so the curve must equal 7: \(x^2 - 2 = 7\).
   - ask: Rearrange to x² =  [box=9, NO label]
   - intro: \(x^2 = 9\), so x is a square root of 9, and there are TWO: \(+3\) and \(−3\).
   - ask: First root (positive), x =  [box=3, NO label]
   - ask: Second root (negative), x =  [box=-3, NO label]
   - ask: Check the curve at x = 3: 3² − 2 =  [box=7, NO label]

## silver[0] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = x + 3\) and \(y = x^2 + 1\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(x + 3 = x^2 + 1\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-1, label:'x']
   - ask: and the constant becomes  [box=-2, NO label]
   - intro: So \(x^2 - x - 2 = 0\). Two numbers multiply to \(-2\) and add to \(-1\): they are \(-2\) and \(+1\), giving \((x - 2)(x + 1) = 0\).
   - ask: First root, x =  [box=2, NO label]
   - ask: Second root, x =  [box=-1, NO label]
   - intro: Now each x gets its y from the line \(y = x + 3\).
   - ask: At x = 2: y =  [box=5, NO label]
   - ask: At x = −1: y =  [box=2, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Check in the curve: 2² + 1 =  [box=5, NO label]

## silver[1] (input: two_solutions, main-box unit: (none))
Q: xyOx²+y²=13y=x+1??Diagram not drawn accuratelySolve \(y = x + 1\) and \(x^2 + y^2 = 13\). Give the two x-values.
   - intro: Substitute \(y = x + 1\) into the circle: \(x^2 + (x + 1)^2 = 13\). Expand the bracket in full.
   - ask: The middle term of (x + 1)², 2 × 1, is  [box=2, label:'x']
   - intro: So \(x^2 + x^2 + 2x + 1 = 13\).
   - ask: Collect the two x² terms, 1 + 1 =  [box=2, label:'x²']
   - intro: That gives \(2x^2 + 2x + 1 = 13\). Take 13 across.
   - ask: The constant, 1 − 13, becomes  [box=-12, NO label]
   - intro: So \(2x^2 + 2x - 12 = 0\). Divide every term by 2 to get \(x^2 + x - 6 = 0\), which factorises as \((x + 3)(x - 2) = 0\).
   - ask: First root, x =  [box=-3, NO label]
   - ask: Second root, x =  [box=2, NO label]
   - intro: Each x gets its y from \(y = x + 1\).
   - ask: At x = −3: y =  [box=-2, NO label]
   - ask: At x = 2: y =  [box=3, NO label]
   - ask: Check (−3, −2) in the circle: (−3)² + (−2)² =  [box=13, NO label]

## silver[2] (input: two_solutions, main-box unit: (none))
Q: xyOx²+y²=10y=x−2??Diagram not drawn accuratelySolve \(y = x - 2\) and \(x^2 + y^2 = 10\). Give the two x-values.
   - intro: Substitute \(y = x - 2\) into the circle: \(x^2 + (x − 2)^2 = 10\). Expand the bracket in full.
   - ask: The middle term of (x − 2)², 2 × (−2), is  [box=-4, label:'x']
   - intro: So \(x^2 + x^2 − 4x + 4 = 10\).
   - ask: Collect the two x² terms, 1 + 1 =  [box=2, label:'x²']
   - intro: That gives \(2x^2 − 4x + 4 = 10\). Take 10 across.
   - ask: The constant, 4 − 10, becomes  [box=-6, NO label]
   - intro: So \(2x^2 - 4x - 6 = 0\). Divide every term by 2 to get \(x^2 - 2x - 3 = 0\), which factorises as \((x + 1)(x - 3) = 0\).
   - ask: First root, x =  [box=-1, NO label]
   - ask: Second root, x =  [box=3, NO label]
   - intro: Each x gets its y from \(y = x - 2\).
   - ask: At x = −1: y =  [box=-3, NO label]
   - ask: At x = 3: y =  [box=1, NO label]
   - ask: Check (−1, −3) in the circle: (−1)² + (−3)² =  [box=10, NO label]

## silver[3] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = x + 5\) and \(y = x^2 + x + 1\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(x + 5 = x^2 + x + 1\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=0, label:'x']
   - ask: and the constant becomes  [box=-4, NO label]
   - intro: So \(x^2 - 4 = 0\). This is a difference of two squares: \((x - 2)(x + 2) = 0\).
   - ask: First root, x =  [box=2, NO label]
   - ask: Second root, x =  [box=-2, NO label]
   - intro: Now each x gets its y from the line \(y = x + 5\).
   - ask: At x = 2: y =  [box=7, NO label]
   - ask: At x = −2: y =  [box=3, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Check in the curve: 2² + 2 + 1 =  [box=7, NO label]

## silver[4] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 3x + 2\) and \(y = x^2 + 2x - 4\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(3x + 2 = x^2 + 2x - 4\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-1, label:'x']
   - ask: and the constant becomes  [box=-6, NO label]
   - intro: So \(x^2 - x - 6 = 0\). Two numbers multiply to \(-6\) and add to \(-1\): they are \(-3\) and \(+2\), giving \((x - 3)(x + 2) = 0\).
   - ask: First root, x =  [box=3, NO label]
   - ask: Second root, x =  [box=-2, NO label]
   - intro: Now each x gets its y from the line \(y = 3x + 2\).
   - ask: At x = 3: y =  [box=11, NO label]
   - ask: At x = −2: y =  [box=-4, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Check in the curve: 3² + 2×3 − 4 =  [box=11, NO label]

## gold[0] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 2 - x\) and \(y = x^2 - 4\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(2 - x = x^2 - 4\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=1, label:'x']
   - ask: and the constant becomes  [box=-6, NO label]
   - intro: So \(x^2 + x - 6 = 0\). Two numbers multiply to \(-6\) and add to \(+1\): they are \(+3\) and \(-2\), giving \((x + 3)(x - 2) = 0\).
   - ask: First root, x =  [box=-3, NO label]
   - ask: Second root, x =  [box=2, NO label]
   - intro: Now each x gets its y from the line \(y = 2 - x\).
   - ask: At x = −3: y =  [box=5, NO label]
   - ask: At x = 2: y =  [box=0, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Check in the curve: (−3)² − 4 =  [box=5, NO label]

## gold[1] (input: two_solutions, main-box unit: (none))
Q: xyOx²+y²=25y=x−1??Diagram not drawn accuratelySolve \(y = x - 1\) and \(x^2 + y^2 = 25\). Give the two x-values.
   - intro: Substitute \(y = x - 1\) into the circle: \(x^2 + (x − 1)^2 = 25\). Expand the bracket in full.
   - ask: The middle term of (x − 1)², 2 × (−1), is  [box=-2, label:'x']
   - intro: So \(x^2 + x^2 − 2x + 1 = 25\).
   - ask: Collect the two x² terms, 1 + 1 =  [box=2, label:'x²']
   - intro: That gives \(2x^2 − 2x + 1 = 25\). Take 25 across.
   - ask: The constant, 1 − 25, becomes  [box=-24, NO label]
   - intro: So \(2x^2 - 2x - 24 = 0\). Divide every term by 2 to get \(x^2 - x - 12 = 0\), which factorises as \((x + 3)(x - 4) = 0\).
   - ask: First root, x =  [box=-3, NO label]
   - ask: Second root, x =  [box=4, NO label]
   - intro: Each x gets its y from \(y = x - 1\).
   - ask: At x = −3: y =  [box=-4, NO label]
   - ask: At x = 4: y =  [box=3, NO label]
   - ask: Check (−3, −4) in the circle: (−3)² + (−4)² =  [box=25, NO label]

## gold[2] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 2x + 3\) and \(y = x^2 + 5x + 3\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(2x + 3 = x^2 + 5x + 3\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=3, label:'x']
   - ask: and the constant becomes  [box=0, NO label]
   - intro: So \(x^2 + 3x = 0\). Take out a common factor of x: \(x(x + 3) = 0\).
   - ask: First root, x =  [box=0, NO label]
   - ask: Second root, x =  [box=-3, NO label]
   - intro: Now each x gets its y from the line \(y = 2x + 3\).
   - ask: At x = 0: y =  [box=3, NO label]
   - ask: At x = −3: y =  [box=-3, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Check in the curve: 0² + 5×0 + 3 =  [box=3, NO label]

## gold[3] (input: two_solutions, main-box unit: (none))
Q: Solve \(x + y = 5\) and \(xy = 6\). Give the two x-values.
   - intro: Make y the subject of the simple equation: \(x + y = 5\) becomes \(y = 5 - x\). Put this into \(xy = 6\): \(x(5 - x) = 6\).
   - intro: So \(5x - x^2 = 6\).
   - ask: Multiply out x(5 − x): the x-term is 5x and the x² term is  [box=-1, label:'x²']
   - intro: So \(x^2 - 5x + 6 = 0\).
   - ask: Move the 6 across and tidy so x² is positive; the constant term becomes  [box=6, NO label]
   - intro: \(x^2 - 5x + 6 = 0\) factorises as \((x - 2)(x - 3) = 0\).
   - ask: First root, x =  [box=2, NO label]
   - ask: Second root, x =  [box=3, NO label]
   - intro: Each x gets its y from \(y = 5 - x\).
   - ask: At x = 2: y = 5 − 2 =  [box=3, NO label]
   - ask: At x = 3: y = 5 − 3 =  [box=2, NO label]
   - ask: Check the product xy for (2, 3): 2 × 3 =  [box=6, NO label]

## gold[4] (input: single_value, main-box unit: (none))
Q: The line \(y = kx + 2\) is a tangent to \(y = x^2 + 3\), so it touches the curve at exactly one point. Find the positive value of k.
   - intro: Where the line meets the curve, \(kx + 2 = x^2 + 3\). Bring all to one side: \(x^2 - kx + 1 = 0\).
   - intro: Tangent means the line touches once, so the discriminant \(b^2 - 4ac\) is zero.
   - ask: For this quadratic, a = 1, c = 1, and b = −k, so b² = k² and 4ac =  [box=4, NO label]
   - intro: So \(k^2 - 4 = 0\).
   - ask: Set k² − 4 = 0, so k² =  [box=4, NO label]
   - ask: The positive value of k is  [box=2, NO label]
   - ask: Check: with k = 2 the quadratic is x² − 2x + 1; its discriminant 2² − 4 =  [box=0, NO label]
