# maths-ocr / algebra / L07 - Solving Quadratics by Factorising

## bronze[0] (input: two_solutions, main-box unit: (none))
Q: Solve \(x^2 + 5x + 6 = 0\)
   - intro: Solve \(x^2 + 5x + 6 = 0\) by finding two numbers that multiply to \(6\) and add to \(5\).
   - ask: The smaller of the two numbers is  [box=2, NO label]
   - ask: The larger of the two numbers is  [box=3, NO label]
   - intro: So it factorises to \((x + 2)(x + 3) = 0\). Each bracket can be zero.
   - ask: First bracket zero: x + 2 = 0, so x =  [box=-2, NO label]
   - ask: Second bracket zero: x + 3 = 0, so x =  [box=-3, NO label]
   - ask: Check x = −2: (−2)² + 5×(−2) + 6 =  [box=0, NO label]

## bronze[1] (input: two_solutions, main-box unit: (none))
Q: Solve \(x^2 + 3x + 2 = 0\)
   - intro: Solve \(x^2 + 3x + 2 = 0\) by finding two numbers that multiply to \(2\) and add to \(3\).
   - ask: The smaller of the two numbers is  [box=1, NO label]
   - ask: The larger of the two numbers is  [box=2, NO label]
   - intro: So it factorises to \((x + 1)(x + 2) = 0\). Each bracket can be zero.
   - ask: First bracket zero: x + 1 = 0, so x =  [box=-1, NO label]
   - ask: Second bracket zero: x + 2 = 0, so x =  [box=-2, NO label]
   - ask: Check x = −1: (−1)² + 3×(−1) + 2 =  [box=0, NO label]

## bronze[2] (input: two_solutions, main-box unit: (none))
Q: Solve \(x^2 - 7x + 10 = 0\)
   - intro: Solve \(x^2 - 7x + 10 = 0\) by finding two numbers that multiply to \(10\) and add to \(-7\).
   - ask: The smaller of the two numbers is  [box=-5, NO label]
   - ask: The larger of the two numbers is  [box=-2, NO label]
   - intro: So it factorises to \((x - 5)(x - 2) = 0\). Each bracket can be zero.
   - ask: First bracket zero: x − 5 = 0, so x =  [box=5, NO label]
   - ask: Second bracket zero: x − 2 = 0, so x =  [box=2, NO label]
   - ask: Check x = 5: (5)² − 7×(5) + 10 =  [box=0, NO label]

## bronze[3] (input: two_solutions, main-box unit: (none))
Q: Solve \(x^2 + 8x + 15 = 0\)
   - intro: Solve \(x^2 + 8x + 15 = 0\) by finding two numbers that multiply to \(15\) and add to \(8\).
   - ask: The smaller of the two numbers is  [box=3, NO label]
   - ask: The larger of the two numbers is  [box=5, NO label]
   - intro: So it factorises to \((x + 3)(x + 5) = 0\). Each bracket can be zero.
   - ask: First bracket zero: x + 3 = 0, so x =  [box=-3, NO label]
   - ask: Second bracket zero: x + 5 = 0, so x =  [box=-5, NO label]
   - ask: Check x = −3: (−3)² + 8×(−3) + 15 =  [box=0, NO label]

## bronze[4] (input: two_solutions, main-box unit: (none))
Q: Solve \(x^2 - 9 = 0\)
   - intro: Solve \(x^2 - 9 = 0\) by finding two numbers that multiply to \(-9\) and add to \(0\).
   - ask: The smaller of the two numbers is  [box=-3, NO label]
   - ask: The larger of the two numbers is  [box=3, NO label]
   - intro: So it factorises to \((x - 3)(x + 3) = 0\). Each bracket can be zero.
   - ask: First bracket zero: x − 3 = 0, so x =  [box=3, NO label]
   - ask: Second bracket zero: x + 3 = 0, so x =  [box=-3, NO label]
   - ask: Check x = 3: (3)² − 9 =  [box=0, NO label]

## bronze[5] (input: two_solutions, main-box unit: (none))
Q: Solve \(x^2 - 4x = 0\)
   - intro: Solve \(x^2 - 4x = 0\). There is no constant, so both terms share the factor \(x\).
   - ask: Divide the second term by x: −4x ÷ x =  [box=-4, NO label]
   - intro: So \(x(x - 4) = 0\). Either factor can be zero.
   - ask: The first factor is just x, so one solution is x =  [box=0, NO label]
   - ask: Second factor: x − 4 = 0, so x =  [box=4, NO label]
   - ask: Check x = 4: (4)² − 4×(4) =  [box=0, NO label]

## bronze[6] (input: two_solutions, main-box unit: (none))
Q: Solve \(x^2 + x - 6 = 0\)
   - intro: Solve \(x^2 + x - 6 = 0\) by finding two numbers that multiply to \(-6\) and add to \(1\).
   - ask: The smaller of the two numbers is  [box=-2, NO label]
   - ask: The larger of the two numbers is  [box=3, NO label]
   - intro: So it factorises to \((x - 2)(x + 3) = 0\). Each bracket can be zero.
   - ask: First bracket zero: x − 2 = 0, so x =  [box=2, NO label]
   - ask: Second bracket zero: x + 3 = 0, so x =  [box=-3, NO label]
   - ask: Check x = 2: (2)² + 1×(2) − 6 =  [box=0, NO label]

## bronze[7] (input: two_solutions, main-box unit: (none))
Q: Solve \(x^2 - 6x + 8 = 0\)
   - intro: Solve \(x^2 - 6x + 8 = 0\) by finding two numbers that multiply to \(8\) and add to \(-6\).
   - ask: The smaller of the two numbers is  [box=-4, NO label]
   - ask: The larger of the two numbers is  [box=-2, NO label]
   - intro: So it factorises to \((x - 4)(x - 2) = 0\). Each bracket can be zero.
   - ask: First bracket zero: x − 4 = 0, so x =  [box=4, NO label]
   - ask: Second bracket zero: x − 2 = 0, so x =  [box=2, NO label]
   - ask: Check x = 4: (4)² − 6×(4) + 8 =  [box=0, NO label]

## silver[0] (input: two_solutions, main-box unit: (none))
Q: Solve \(x^2 - 2x - 15 = 0\)
   - intro: Solve \(x^2 - 2x - 15 = 0\) by finding two numbers that multiply to \(-15\) and add to \(-2\).
   - ask: The smaller of the two numbers is  [box=-5, NO label]
   - ask: The larger of the two numbers is  [box=3, NO label]
   - intro: So it factorises to \((x - 5)(x + 3) = 0\). Each bracket can be zero.
   - ask: First bracket zero: x − 5 = 0, so x =  [box=5, NO label]
   - ask: Second bracket zero: x + 3 = 0, so x =  [box=-3, NO label]
   - ask: Check x = 5: (5)² − 2×(5) − 15 =  [box=0, NO label]

## silver[1] (input: two_solutions, main-box unit: (none))
Q: Solve \(x^2 + 4x - 21 = 0\)
   - intro: Solve \(x^2 + 4x - 21 = 0\) by finding two numbers that multiply to \(-21\) and add to \(4\).
   - ask: The smaller of the two numbers is  [box=-3, NO label]
   - ask: The larger of the two numbers is  [box=7, NO label]
   - intro: So it factorises to \((x - 3)(x + 7) = 0\). Each bracket can be zero.
   - ask: First bracket zero: x − 3 = 0, so x =  [box=3, NO label]
   - ask: Second bracket zero: x + 7 = 0, so x =  [box=-7, NO label]
   - ask: Check x = 3: (3)² + 4×(3) − 21 =  [box=0, NO label]

## silver[2] (input: two_solutions, main-box unit: (none))
Q: Solve \(2x^2 + 3x - 2 = 0\)
   - intro: With a coefficient on \(x^2\), use split the middle. Multiply the ends: \(2 \times (-2) = -4\).
   - ask: Two numbers multiply to −4 and add to 3: 4 and  [box=-1, NO label]
   - intro: Grouping gives \((2x - 1)(x + 2) = 0\).
   - ask: Split: 2x² + 4x − x − 2, group to 2x(x+2) − 1(x+2) = (2x − 1)(x + 2). Set 2x − 1 = 0: 2x = 1, x =  [box=0.5, NO label]
   - ask: Set x + 2 = 0: x =  [box=-2, NO label]
   - ask: Check x = 0.5: 2×(0.5)² + 3×(0.5) − 2 =  [box=0, NO label]

## silver[3] (input: two_solutions, main-box unit: (none))
Q: Solve \(x^2 = 5x + 14\)
   - intro: First rearrange so one side is 0. Move every term on the right across to the left; each changes sign.
   - ask: The 5x on the right moves left and becomes  [box=-5, label:'x']
   - ask: The 14 on the right moves left and becomes  [box=-14, NO label]
   - intro: So \(x^2 - 5x - 14 = 0\). Now two numbers multiply to \(-14\) and add to \(-5\).
   - ask: The smaller of the two numbers is  [box=-7, NO label]
   - ask: The larger of the two numbers is  [box=2, NO label]
   - intro: So \((x - 7)(x + 2) = 0\).
   - ask: First bracket zero: x − 7 = 0, so x =  [box=7, NO label]
   - ask: Second bracket zero: x + 2 = 0, so x =  [box=-2, NO label]
   - ask: Check x = 7: (7)² − 5×(7) − 14 =  [box=0, NO label]

## silver[4] (input: two_solutions, main-box unit: (none))
Q: Solve \(x^2 - 25 = 0\)
   - intro: Solve \(x^2 - 25 = 0\) by finding two numbers that multiply to \(-25\) and add to \(0\).
   - ask: The smaller of the two numbers is  [box=-5, NO label]
   - ask: The larger of the two numbers is  [box=5, NO label]
   - intro: So it factorises to \((x - 5)(x + 5) = 0\). Each bracket can be zero.
   - ask: First bracket zero: x − 5 = 0, so x =  [box=5, NO label]
   - ask: Second bracket zero: x + 5 = 0, so x =  [box=-5, NO label]
   - ask: Check x = 5: (5)² − 25 =  [box=0, NO label]

## silver[5] (input: two_solutions, main-box unit: (none))
Q: Solve \(3x^2 - 12x = 0\)
   - intro: Solve \(3x^2 - 12x = 0\). Both terms share the common factor \(3x\).
   - ask: Divide the second term by 3x: 12x ÷ 3x =  [box=4, NO label]
   - intro: So \(3x(x - 4) = 0\). Either factor can be zero.
   - ask: First factor: 3x = 0, so x =  [box=0, NO label]
   - ask: Second factor: x − 4 = 0, so x =  [box=4, NO label]
   - ask: Check x = 4: 3×(4)² − 12×(4) =  [box=0, NO label]

## silver[6] (input: two_solutions, main-box unit: (none))
Q: Solve \(x^2 - 10x + 25 = 0\)
   - intro: Solve \(x^2 - 10x + 25 = 0\). Two equal numbers multiply to 25 and add to \(-10\).
   - ask: The first of the two equal numbers is  [box=-5, NO label]
   - ask: The second (equal) number is  [box=-5, NO label]
   - intro: So \((x - 5)(x - 5) = (x - 5)^2 = 0\). A perfect square: the root is repeated.
   - ask: x − 5 = 0, so x =  [box=5, NO label]
   - ask: The root is repeated, so the second solution is also x =  [box=5, NO label]
   - ask: Check x = 5: (5)² − 10×(5) + 25 =  [box=0, NO label]

## gold[0] (input: two_solutions, main-box unit: (none))
Q: Solve \(4x^2 + 4x - 3 = 0\)
   - intro: With a coefficient on \(x^2\), use split the middle. Multiply the ends: \(4 \times (-3) = -12\).
   - ask: Two numbers multiply to −12 and add to 4: 6 and  [box=-2, NO label]
   - intro: Grouping gives \((2x + 3)(2x - 1) = 0\).
   - ask: Split: 4x² + 6x − 2x − 3, group to 2x(2x+3) − 1(2x+3) = (2x + 3)(2x − 1). Set 2x − 1 = 0: 2x = 1, x =  [box=0.5, NO label]
   - ask: Set 2x + 3 = 0: 2x = −3, x =  [box=-1.5, NO label]
   - ask: Check x = 0.5: 4×(0.5)² + 4×(0.5) − 3 =  [box=0, NO label]

## gold[1] (input: two_solutions, main-box unit: (none))
Q: Solve \(4x^2 - 1 = 0\)
   - intro: There is no middle term, so this is a difference of two squares: \(4x^2\) is \((2x)^2\) and \(1\) is \(1^2\).
   - ask: So it factorises to (2x + 1)(2x − 1) = 0. Set 2x − 1 = 0: 2x = 1, x =  [box=0.5, NO label]
   - ask: Set 2x + 1 = 0: 2x = −1, x =  [box=-0.5, NO label]
   - ask: Check x = 0.5: 4×(0.5)² − 1 =  [box=0, NO label]

## gold[2] (input: two_solutions, main-box unit: (none))
Q: Solve \(2x^2 + 7x + 3 = 0\)
   - intro: With a coefficient on \(x^2\), use split the middle. Multiply the ends: \(2 \times 3 = 6\).
   - ask: Two numbers multiply to 6 and add to 7: 6 and  [box=1, NO label]
   - intro: Grouping gives \((2x + 1)(x + 3) = 0\).
   - ask: Split: 2x² + 6x + x + 3, group to 2x(x+3) + 1(x+3) = (2x + 1)(x + 3). Set 2x + 1 = 0: 2x = −1, x =  [box=-0.5, NO label]
   - ask: Set x + 3 = 0: x =  [box=-3, NO label]
   - ask: Check x = −0.5: 2×(−0.5)² + 7×(−0.5) + 3 =  [box=0, NO label]

## gold[3] (input: two_solutions, main-box unit: (none))
Q: Solve \(x^2 + 3x = 18\)
   - intro: First rearrange so one side is 0. Subtract 18 from both sides.
   - ask: The 18 moves left and becomes  [box=-18, NO label]
   - intro: So \(x^2 + 3x - 18 = 0\). Now two numbers multiply to \(-18\) and add to \(3\).
   - ask: The smaller of the two numbers is  [box=-3, NO label]
   - ask: The larger of the two numbers is  [box=6, NO label]
   - intro: So \((x - 3)(x + 6) = 0\).
   - ask: First bracket zero: x − 3 = 0, so x =  [box=3, NO label]
   - ask: Second bracket zero: x + 6 = 0, so x =  [box=-6, NO label]
   - ask: Check x = 3: (3)² + 3×(3) − 18 =  [box=0, NO label]

## gold[4] (input: two_solutions, main-box unit: (none))
Q: Solve \(5x^2 - 3x - 2 = 0\)
   - intro: With a coefficient on \(x^2\), use split the middle. Multiply the ends: \(5 \times (-2) = -10\).
   - ask: Two numbers multiply to −10 and add to −3: 2 and  [box=-5, NO label]
   - intro: Grouping gives \((5x + 2)(x - 1) = 0\).
   - ask: Split: 5x² − 5x + 2x − 2, group to 5x(x − 1) + 2(x − 1) = (5x + 2)(x − 1). Set x − 1 = 0: x =  [box=1, NO label]
   - ask: Set 5x + 2 = 0: 5x = −2, x =  [box=-0.4, NO label]
   - ask: Check x = 1: 5×(1)² − 3×(1) − 2 =  [box=0, NO label]
