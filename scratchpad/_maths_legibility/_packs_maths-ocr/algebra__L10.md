# maths-ocr / algebra / L10 - Simultaneous Equations (Quadratic)

## bronze[0] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = x + 1\) and \(y = x^2 - 2x - 3\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(x + 1 = x^2 - 2x - 3\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-3, label:'x']
   - ask: and the constant becomes  [box=-4, NO label]
   - intro: So \(x^2 - 3x - 4 = 0\). Two numbers multiply to −4 and add to −3: they are −4 and 1, giving \((x - 4)(x + 1) = 0\).
   - ask: First root, x =  [box=4, NO label]
   - ask: Second root, x =  [box=-1, NO label]
   - intro: Now each x gets its y from the line \(y = x + 1\).
   - ask: At x = 4: y =  [box=5, NO label]
   - ask: At x = −1: y =  [box=0, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Work out 4² − 2×4 − 3 =  [box=5, NO label]

## bronze[1] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 3x\) and \(y = x^2 + 2\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(3x = x^2 + 2\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-3, label:'x']
   - ask: and the constant becomes  [box=2, NO label]
   - intro: So \(x^2 - 3x + 2 = 0\). Two numbers multiply to 2 and add to −3: they are −1 and −2, giving \((x - 1)(x - 2) = 0\).
   - ask: First root, x =  [box=1, NO label]
   - ask: Second root, x =  [box=2, NO label]
   - intro: Now each x gets its y from the line \(y = 3x\).
   - ask: At x = 1: y =  [box=3, NO label]
   - ask: At x = 2: y =  [box=6, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Work out 1² + 2 =  [box=3, NO label]

## bronze[2] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = x + 2\) and \(y = x^2\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(x + 2 = x^2\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-1, label:'x']
   - ask: and the constant becomes  [box=-2, NO label]
   - intro: So \(x^2 - x - 2 = 0\). Two numbers multiply to −2 and add to −1: they are −2 and 1, giving \((x - 2)(x + 1) = 0\).
   - ask: First root, x =  [box=2, NO label]
   - ask: Second root, x =  [box=-1, NO label]
   - intro: Now each x gets its y from the line \(y = x + 2\).
   - ask: At x = 2: y =  [box=4, NO label]
   - ask: At x = −1: y =  [box=1, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Work out 2² =  [box=4, NO label]

## bronze[3] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 2x\) and \(y = x^2 - 3\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(2x = x^2 - 3\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-2, label:'x']
   - ask: and the constant becomes  [box=-3, NO label]
   - intro: So \(x^2 - 2x - 3 = 0\). Two numbers multiply to −3 and add to −2: they are −3 and 1, giving \((x - 3)(x + 1) = 0\).
   - ask: First root, x =  [box=3, NO label]
   - ask: Second root, x =  [box=-1, NO label]
   - intro: Now each x gets its y from the line \(y = 2x\).
   - ask: At x = 3: y =  [box=6, NO label]
   - ask: At x = −1: y =  [box=-2, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Work out 3² − 3 =  [box=6, NO label]

## bronze[4] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = x + 5\) and \(y = x^2 + 3x + 2\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(x + 5 = x^2 + 3x + 2\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=2, label:'x']
   - ask: and the constant becomes  [box=-3, NO label]
   - intro: So \(x^2 + 2x - 3 = 0\). Two numbers multiply to −3 and add to 2: they are −1 and 3, giving \((x - 1)(x + 3) = 0\).
   - ask: First root, x =  [box=1, NO label]
   - ask: Second root, x =  [box=-3, NO label]
   - intro: Now each x gets its y from the line \(y = x + 5\).
   - ask: At x = 1: y =  [box=6, NO label]
   - ask: At x = −3: y =  [box=2, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Work out 1² + 3×1 + 2 =  [box=6, NO label]

## bronze[5] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 2x + 3\) and \(y = x^2 + 3\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(2x + 3 = x^2 + 3\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-2, label:'x']
   - ask: and the constant becomes  [box=0, NO label]
   - intro: So \(x^2 - 2x = 0\). Take out a common factor of x: \(x(x - 2) = 0\).
   - ask: First root, x =  [box=0, NO label]
   - ask: Second root, x =  [box=2, NO label]
   - intro: Now each x gets its y from the line \(y = 2x + 3\).
   - ask: At x = 0: y =  [box=3, NO label]
   - ask: At x = 2: y =  [box=7, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Work out 0² + 3 =  [box=3, NO label]

## bronze[6] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = x - 1\) and \(y = x^2 - 4x + 5\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(x - 1 = x^2 - 4x + 5\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-5, label:'x']
   - ask: and the constant becomes  [box=6, NO label]
   - intro: So \(x^2 - 5x + 6 = 0\). Two numbers multiply to 6 and add to −5: they are −2 and −3, giving \((x - 2)(x - 3) = 0\).
   - ask: First root, x =  [box=2, NO label]
   - ask: Second root, x =  [box=3, NO label]
   - intro: Now each x gets its y from the line \(y = x - 1\).
   - ask: At x = 2: y =  [box=1, NO label]
   - ask: At x = 3: y =  [box=2, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Work out 2² − 4×2 + 5 =  [box=1, NO label]

## bronze[7] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 4 - x\) and \(y = x^2 - 2\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(4 - x = x^2 - 2\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=1, label:'x']
   - ask: and the constant becomes  [box=-6, NO label]
   - intro: So \(x^2 + x - 6 = 0\). Two numbers multiply to −6 and add to 1: they are −2 and 3, giving \((x - 2)(x + 3) = 0\).
   - ask: First root, x =  [box=2, NO label]
   - ask: Second root, x =  [box=-3, NO label]
   - intro: Now each x gets its y from the line \(y = 4 - x\).
   - ask: At x = 2: y =  [box=2, NO label]
   - ask: At x = −3: y =  [box=7, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Work out 2² − 2 =  [box=2, NO label]

## silver[0] (input: two_solutions, main-box unit: (none))
Q: xyOx²+y²=13x+y=5??Diagram not drawn accuratelySolve \(x + y = 5\) and \(x^2 + y^2 = 13\). Give the two x-values.
   - intro: Rearrange the line \(x + y = 5\) to \(y = 5 - x\). Substitute into \(x^2 + y^2 = 13\): \(x^2 + (5 - x)^2 = 13\). Expand \((5 - x)^2 = 25 - 10x + x^2\).
   - ask: The middle term, 2 × 5 × (−1), is  [box=-10, label:'x']
   - ask: Collect the two x² terms: 1 + 1 =  [box=2, label:'x²']
   - ask: The constant, 25 − 13, becomes  [box=12, NO label]
   - intro: So \(2x^2 - 10x + 12 = 0\). Divide every term by 2 to get \(x^2 - 5x + 6 = 0\), which factorises as \((x - 2)(x - 3) = 0\).
   - ask: First root, x =  [box=2, NO label]
   - ask: Second root, x =  [box=3, NO label]
   - intro: Each x gets its y from \(y = 5 - x\).
   - ask: At x = 2: y =  [box=3, NO label]
   - ask: At x = 3: y =  [box=2, NO label]
   - ask: Check (2, 3) in the circle: 2² + 3² =  [box=13, NO label]

## silver[1] (input: two_solutions, main-box unit: (none))
Q: xyOx²+y²=10y=2x−1??Diagram not drawn accuratelySolve \(y = 2x - 1\) and \(x^2 + y^2 = 10\). Give the two x-values.
   - intro: Substitute \(y = 2x - 1\) into the circle \(x^2 + y^2 = 10\): \(x^2 + (2x - 1)^2 = 10\). Expand \((2x - 1)^2 = 4x^2 - 4x + 1\).
   - ask: The middle term, 2 × 2x × (−1), is  [box=-4, label:'x']
   - intro: So \(x^2 + 4x^2 - 4x + 1 = 10\).
   - ask: Collect the x² terms: 1 + 4 =  [box=5, label:'x²']
   - intro: That gives \(5x^2 - 4x + 1 = 10\). Take 10 across.
   - ask: The constant, 1 − 10, becomes  [box=-9, NO label]
   - intro: So \(5x^2 - 4x - 9 = 0\). This factorises as \((5x - 9)(x + 1) = 0\).
   - ask: From x + 1 = 0, one root is x =  [box=-1, NO label]
   - intro: Then \(5x = 9\).
   - ask: From 5x − 9 = 0, the other root is x =  [box=1.8, NO label]
   - intro: Each x gets its y from \(y = 2x - 1\).
   - ask: At x = 1.8: y =  [box=2.6, NO label]
   - ask: At x = −1: y =  [box=-3, NO label]
   - ask: Check (1.8, 2.6) in the circle: 1.8² + 2.6² =  [box=10, NO label]

## silver[2] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = x + 3\) and \(xy = 10\). Give the two x-values.
   - intro: Substitute \(y = x + 3\) into \(xy = 10\): \(x(x + 3) = 10\), which expands to \(x^2 + 3x = 10\).
   - ask: Take 10 across so the equation is x² + 3x −  [box=10, label:' = 0']
   - intro: So \(x^2 + 3x - 10 = 0\). Two numbers multiply to −10 and add to 3: they are 5 and −2, giving \((x + 5)(x - 2) = 0\).
   - ask: First root, x =  [box=2, NO label]
   - ask: Second root, x =  [box=-5, NO label]
   - intro: Each x gets its y from \(y = x + 3\).
   - ask: At x = 2: y =  [box=5, NO label]
   - ask: At x = −5: y =  [box=-2, NO label]
   - ask: Check by multiplying: 2 × 5 =  [box=10, NO label]

## silver[3] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 4 - 2x\) and \(x^2 + xy = 3\). Give the two x-values.
   - intro: Substitute \(y = 4 - 2x\) into \(x^2 + xy = 3\): \(x^2 + x(4 - 2x) = 3\). Expand the bracket: \(x^2 + 4x - 2x^2 = 3\).
   - ask: Expanding x(4 − 2x) gives 4x − 2x². Add the x²: 1 − 2 =  [box=-1, label:'x²']
   - intro: So \(-x^2 + 4x = 3\). Multiply through by −1 so \(x^2\) is positive.
   - ask: Multiplying −x² + 4x = 3 by −1, the x-term becomes  [box=-4, label:'x']
   - intro: That gives \(x^2 - 4x = -3\).
   - ask: and moving the constant across, the equation is x² − 4x +  [box=3, label:' = 0']
   - intro: \(x^2 - 4x + 3 = 0\) factorises as \((x - 1)(x - 3) = 0\).
   - ask: First root, x =  [box=1, NO label]
   - ask: Second root, x =  [box=3, NO label]
   - intro: Each x gets its y from \(y = 4 - 2x\).
   - ask: At x = 1: y =  [box=2, NO label]
   - ask: At x = 3: y =  [box=-2, NO label]
   - ask: Check (1, 2): x² + xy = 1 + 1×2 =  [box=3, NO label]

## silver[4] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = x + 1\) and \(y = x^2 - 3x + 1\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(x + 1 = x^2 - 3x + 1\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-4, label:'x']
   - ask: and the constant becomes  [box=0, NO label]
   - intro: So \(x^2 - 4x = 0\). Take out a common factor of x: \(x(x - 4) = 0\).
   - ask: First root, x =  [box=0, NO label]
   - ask: Second root, x =  [box=4, NO label]
   - intro: Now each x gets its y from the line \(y = x + 1\).
   - ask: At x = 0: y =  [box=1, NO label]
   - ask: At x = 4: y =  [box=5, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Work out 0² − 3×0 + 1 =  [box=1, NO label]

## silver[5] (input: two_solutions, main-box unit: (none))
Q: xyOx²+y²=20x−y=2??Diagram not drawn accuratelySolve \(x - y = 2\) and \(x^2 + y^2 = 20\). Give the two x-values.
   - intro: Rearrange the line \(x - y = 2\) to \(y = x - 2\). Substitute into \(x^2 + y^2 = 20\): \(x^2 + (x - 2)^2 = 20\). Expand \((x - 2)^2 = x^2 - 4x + 4\).
   - ask: The middle term, 2 × x × (−2), is  [box=-4, label:'x']
   - ask: Collect the two x² terms: 1 + 1 =  [box=2, label:'x²']
   - ask: The constant, 4 − 20, becomes  [box=-16, NO label]
   - intro: So \(2x^2 - 4x - 16 = 0\). Divide every term by 2 to get \(x^2 - 2x - 8 = 0\), which factorises as \((x - 4)(x + 2) = 0\).
   - ask: First root, x =  [box=4, NO label]
   - ask: Second root, x =  [box=-2, NO label]
   - intro: Each x gets its y from \(y = x - 2\).
   - ask: At x = 4: y =  [box=2, NO label]
   - ask: At x = −2: y =  [box=-4, NO label]
   - ask: Check (4, 2) in the circle: 4² + 2² =  [box=20, NO label]

## silver[6] (input: two_solutions, main-box unit: (none))
Q: xyOx²+y²=20y=2x??Diagram not drawn accuratelySolve \(y = 2x\) and \(x^2 + y^2 = 20\). Give the two x-values.
   - intro: Substitute \(y = 2x\) into \(x^2 + y^2 = 20\): \(x^2 + (2x)^2 = 20\), and \((2x)^2 = 4x^2\).
   - ask: Squaring 2x gives 4x². Collect x² terms: 1 + 4 =  [box=5, label:'x²']
   - intro: There is no x-term this time, so just divide by 5.
   - ask: So 5x² = 20. Then x² =  [box=4, NO label]
   - intro: \(x^2 = 4\) has two roots, one positive and one negative.
   - ask: Positive root, x =  [box=2, NO label]
   - ask: Negative root, x =  [box=-2, NO label]
   - intro: Each x gets its y from \(y = 2x\).
   - ask: At x = 2: y =  [box=4, NO label]
   - ask: At x = −2: y =  [box=-4, NO label]
   - ask: Check (2, 4) in the circle: 2² + 4² =  [box=20, NO label]

## gold[0] (input: two_solutions, main-box unit: (none))
Q: xyOx²+y²=13x+y=1??Diagram not drawn accuratelySolve \(x + y = 1\) and \(x^2 + y^2 = 13\). Give the two x-values.
   - intro: Rearrange the line \(x + y = 1\) to \(y = 1 - x\). Substitute into \(x^2 + y^2 = 13\): \(x^2 + (1 - x)^2 = 13\). Expand \((1 - x)^2 = 1 - 2x + x^2\).
   - ask: The middle term, 2 × 1 × (−1), is  [box=-2, label:'x']
   - ask: Collect the two x² terms: 1 + 1 =  [box=2, label:'x²']
   - ask: The constant, 1 − 13, becomes  [box=-12, NO label]
   - intro: So \(2x^2 - 2x - 12 = 0\). Divide every term by 2 to get \(x^2 - 1x - 6 = 0\), which factorises as \((x - 3)(x + 2) = 0\).
   - ask: First root, x =  [box=3, NO label]
   - ask: Second root, x =  [box=-2, NO label]
   - intro: Each x gets its y from \(y = 1 - x\).
   - ask: At x = 3: y =  [box=-2, NO label]
   - ask: At x = −2: y =  [box=3, NO label]
   - ask: Check (3, −2) in the circle: 3² + −2² =  [box=13, NO label]

## gold[1] (input: two_solutions, main-box unit: (none))
Q: xyOx²+y²=5y=3x−1??Diagram not drawn accuratelySolve \(y = 3x - 1\) and \(x^2 + y^2 = 5\). Give the two x-values.
   - intro: Substitute \(y = 3x - 1\) into the circle \(x^2 + y^2 = 5\): \(x^2 + (3x - 1)^2 = 5\). Expand \((3x - 1)^2 = 9x^2 - 6x + 1\).
   - ask: The middle term, 2 × 3x × (−1), is  [box=-6, label:'x']
   - intro: So \(x^2 + 9x^2 - 6x + 1 = 5\).
   - ask: Collect the x² terms: 1 + 9 =  [box=10, label:'x²']
   - intro: That gives \(10x^2 - 6x + 1 = 5\). Take 5 across.
   - ask: The constant, 1 − 5, becomes  [box=-4, NO label]
   - intro: So \(10x^2 - 6x - 4 = 0\). Divide every term by 2 to get \(5x^2 - 3x - 2 = 0\), which factorises as \((5x + 2)(x - 1) = 0\).
   - ask: First root, x =  [box=1, NO label]
   - intro: Then \(5x = -2\).
   - ask: From 5x + 2 = 0, the second root is x =  [box=-0.4, NO label]
   - intro: Each x gets its y from \(y = 3x - 1\).
   - ask: At x = 1: y =  [box=2, NO label]
   - ask: At x = −0.4: y =  [box=-2.2, NO label]
   - ask: Check (1, 2) in the circle: 1² + 2² =  [box=5, NO label]

## gold[2] (input: two_solutions, main-box unit: (none))
Q: xyOx²+y²=17x+y=5??Diagram not drawn accuratelySolve \(x + y = 5\) and \(x^2 + y^2 = 17\). Give the two x-values.
   - intro: Rearrange the line \(x + y = 5\) to \(y = 5 - x\). Substitute into \(x^2 + y^2 = 17\): \(x^2 + (5 - x)^2 = 17\). Expand \((5 - x)^2 = 25 - 10x + x^2\).
   - ask: The middle term, 2 × 5 × (−1), is  [box=-10, label:'x']
   - ask: Collect the two x² terms: 1 + 1 =  [box=2, label:'x²']
   - ask: The constant, 25 − 17, becomes  [box=8, NO label]
   - intro: So \(2x^2 - 10x + 8 = 0\). Divide every term by 2 to get \(x^2 - 5x + 4 = 0\), which factorises as \((x - 1)(x - 4) = 0\).
   - ask: First root, x =  [box=1, NO label]
   - ask: Second root, x =  [box=4, NO label]
   - intro: Each x gets its y from \(y = 5 - x\).
   - ask: At x = 1: y =  [box=4, NO label]
   - ask: At x = 4: y =  [box=1, NO label]
   - ask: Check (1, 4) in the circle: 1² + 4² =  [box=17, NO label]

## gold[3] (input: two_solutions, main-box unit: (none))
Q: Solve \(2x + y = 5\) and \(xy = 2\). Give the two x-values.
   - intro: Rearrange the line \(2x + y = 5\) to \(y = 5 - 2x\). Substitute into \(xy = 2\): \(x(5 - 2x) = 2\), which expands to \(5x - 2x^2 = 2\).
   - ask: Expanding x(5 − 2x) gives 5x − 2x². Bring 2 across so 2x² is positive: 2x² − 5x +  [box=2, label:' = 0']
   - intro: So \(2x^2 - 5x + 2 = 0\). This factorises as \((2x - 1)(x - 2) = 0\).
   - ask: First root, x =  [box=2, NO label]
   - intro: Then \(2x = 1\).
   - ask: From 2x − 1 = 0, the second root is x =  [box=0.5, NO label]
   - intro: Each x gets its y from \(y = 5 - 2x\).
   - ask: At x = 2: y =  [box=1, NO label]
   - ask: At x = 0.5: y =  [box=4, NO label]
   - ask: Check by multiplying: 2 × 1 =  [box=2, NO label]

## gold[4] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 5 - 2x\) and \(x^2 + xy = 6\). Give the two x-values.
   - intro: Substitute \(y = 5 - 2x\) into \(x^2 + xy = 6\): \(x^2 + x(5 - 2x) = 6\). Expand the bracket: \(x^2 + 5x - 2x^2 = 6\).
   - ask: Expanding x(5 − 2x) gives 5x − 2x². Add the x²: 1 − 2 =  [box=-1, label:'x²']
   - intro: So \(-x^2 + 5x = 6\). Multiply through by −1 so \(x^2\) is positive.
   - ask: Multiplying −x² + 5x = 6 by −1, the x-term becomes  [box=-5, label:'x']
   - intro: That gives \(x^2 - 5x = -6\).
   - ask: and moving the constant across, the equation is x² − 5x +  [box=6, label:' = 0']
   - intro: \(x^2 - 5x + 6 = 0\) factorises as \((x - 2)(x - 3) = 0\).
   - ask: First root, x =  [box=2, NO label]
   - ask: Second root, x =  [box=3, NO label]
   - intro: Each x gets its y from \(y = 5 - 2x\).
   - ask: At x = 2: y =  [box=1, NO label]
   - ask: At x = 3: y =  [box=-1, NO label]
   - ask: Check (2, 1): x² + xy = 4 + 2×1 =  [box=6, NO label]
