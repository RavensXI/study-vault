# maths-eduqas / algebra / L10 - Simultaneous Equations (Quadratic)

## bronze[0] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = x\) and \(y = x^2\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(x = x^2\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-1, label:'x']
   - intro: So \(x^2 - x = 0\). Take out a common factor of x: \(x(x - 1) = 0\).
   - ask: There is no constant, so factor out x. First root, x =  [box=0, NO label]
   - ask: Second root, x =  [box=1, NO label]
   - intro: Now each x gets its y from the line \(x\).
   - ask: At x = 0: y =  [box=0, NO label]
   - ask: At x = 1: y =  [box=1, NO label]
   - ask: Check: 1^2 =  [box=1, NO label]

## bronze[1] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 2x\) and \(y = x^2\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(2x = x^2\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-2, label:'x']
   - intro: So \(x^2 - 2x = 0\). Take out a common factor of x: \(x(x - 2) = 0\).
   - ask: There is no constant, so factor out x. First root, x =  [box=0, NO label]
   - ask: Second root, x =  [box=2, NO label]
   - intro: Now each x gets its y from the line \(2x\).
   - ask: At x = 0: y =  [box=0, NO label]
   - ask: At x = 2: y =  [box=4, NO label]
   - ask: Check: 2^2 =  [box=4, NO label]

## bronze[2] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 3\) and \(y = x^2 - 1\). Give the two x-values.
   - intro: Both sides give y, so set them equal: \(3 = x^2 - 1\). Get \(x^2\) on its own.
   - ask: So x² =  [box=4, NO label]
   - intro: \(x^2 = 4\) has TWO roots, one positive and one negative.
   - ask: First root (positive), x =  [box=2, NO label]
   - ask: Second root (negative), x =  [box=-2, NO label]
   - intro: The line is flat, so y is the same at both points.
   - ask: Both roots give the same y. y =  [box=3, NO label]
   - ask: Check: 2^2 - 1 =  [box=3, NO label]

## bronze[3] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = x + 2\) and \(y = x^2\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(x + 2 = x^2\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-1, label:'x']
   - ask: and the constant becomes  [box=-2, NO label]
   - intro: So \(x^2 - x - 2 = 0\). Two numbers multiply to -2 and add to -1: they are -2 and 1, giving \((x - 2)(x + 1) = 0\).
   - ask: First root, x =  [box=2, NO label]
   - ask: Second root, x =  [box=-1, NO label]
   - intro: Now each x gets its y from the line \(x + 2\).
   - ask: At x = 2: y =  [box=4, NO label]
   - ask: At x = -1: y =  [box=1, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Check: 2^2 =  [box=4, NO label]

## bronze[4] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 10\) and \(y = x^2 + 1\). Give the two x-values.
   - intro: Both sides give y, so set them equal: \(10 = x^2 + 1\). Get \(x^2\) on its own.
   - ask: So x² =  [box=9, NO label]
   - intro: \(x^2 = 9\) has TWO roots, one positive and one negative.
   - ask: First root (positive), x =  [box=3, NO label]
   - ask: Second root (negative), x =  [box=-3, NO label]
   - intro: The line is flat, so y is the same at both points.
   - ask: Both roots give the same y. y =  [box=10, NO label]
   - ask: Check: 3^2 + 1 =  [box=10, NO label]

## bronze[5] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = x + 6\) and \(y = x^2\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(x + 6 = x^2\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-1, label:'x']
   - ask: and the constant becomes  [box=-6, NO label]
   - intro: So \(x^2 - x - 6 = 0\). Two numbers multiply to -6 and add to -1: they are -3 and 2, giving \((x - 3)(x + 2) = 0\).
   - ask: First root, x =  [box=3, NO label]
   - ask: Second root, x =  [box=-2, NO label]
   - intro: Now each x gets its y from the line \(x + 6\).
   - ask: At x = 3: y =  [box=9, NO label]
   - ask: At x = -2: y =  [box=4, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Check: 3^2 =  [box=9, NO label]

## bronze[6] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = -x\) and \(y = x^2 - 2\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(-x = x^2 - 2\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=1, label:'x']
   - ask: and the constant becomes  [box=-2, NO label]
   - intro: So \(x^2 + x - 2 = 0\). Two numbers multiply to -2 and add to 1: they are 2 and -1, giving \((x + 2)(x - 1) = 0\).
   - ask: First root, x =  [box=-2, NO label]
   - ask: Second root, x =  [box=1, NO label]
   - intro: Now each x gets its y from the line \(-x\).
   - ask: At x = -2: y =  [box=2, NO label]
   - ask: At x = 1: y =  [box=-1, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Check: (-2)^2 - 2 =  [box=2, NO label]

## bronze[7] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 4x\) and \(y = x^2 + 3\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(4x = x^2 + 3\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-4, label:'x']
   - ask: and the constant becomes  [box=3, NO label]
   - intro: So \(x^2 - 4x + 3 = 0\). Two numbers multiply to 3 and add to -4: they are -1 and -3, giving \((x - 1)(x - 3) = 0\).
   - ask: First root, x =  [box=1, NO label]
   - ask: Second root, x =  [box=3, NO label]
   - intro: Now each x gets its y from the line \(4x\).
   - ask: At x = 1: y =  [box=4, NO label]
   - ask: At x = 3: y =  [box=12, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Check: 1^2 + 3 =  [box=4, NO label]

## silver[0] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = x + 3\) and \(y = x^2 + 1\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(x + 3 = x^2 + 1\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-1, label:'x']
   - ask: and the constant becomes  [box=-2, NO label]
   - intro: So \(x^2 - x - 2 = 0\). Two numbers multiply to -2 and add to -1: they are -2 and 1, giving \((x - 2)(x + 1) = 0\).
   - ask: First root, x =  [box=2, NO label]
   - ask: Second root, x =  [box=-1, NO label]
   - intro: Now each x gets its y from the line \(x + 3\).
   - ask: At x = 2: y =  [box=5, NO label]
   - ask: At x = -1: y =  [box=2, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Check: 2^2 + 1 =  [box=5, NO label]

## silver[1] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 2 - x\) and \(y = x^2 - 4\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(2 - x = x^2 - 4\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=1, label:'x']
   - ask: and the constant becomes  [box=-6, NO label]
   - intro: So \(x^2 + x - 6 = 0\). Two numbers multiply to -6 and add to 1: they are -2 and 3, giving \((x - 2)(x + 3) = 0\).
   - ask: First root, x =  [box=2, NO label]
   - ask: Second root, x =  [box=-3, NO label]
   - intro: Now each x gets its y from the line \(2 - x\).
   - ask: At x = 2: y =  [box=0, NO label]
   - ask: At x = -3: y =  [box=5, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Check: 2^2 - 4 =  [box=0, NO label]

## silver[2] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 2x - 1\) and \(y = x^2 - 2x + 2\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(2x - 1 = x^2 - 2x + 2\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-4, label:'x']
   - ask: and the constant becomes  [box=3, NO label]
   - intro: So \(x^2 - 4x + 3 = 0\). Two numbers multiply to 3 and add to -4: they are -1 and -3, giving \((x - 1)(x - 3) = 0\).
   - ask: First root, x =  [box=1, NO label]
   - ask: Second root, x =  [box=3, NO label]
   - intro: Now each x gets its y from the line \(2x - 1\).
   - ask: At x = 1: y =  [box=1, NO label]
   - ask: At x = 3: y =  [box=5, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Check: 1^2 - 2 + 2 =  [box=1, NO label]

## silver[3] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = x + 1\) and \(y = x^2 - x - 2\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(x + 1 = x^2 - x - 2\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-2, label:'x']
   - ask: and the constant becomes  [box=-3, NO label]
   - intro: So \(x^2 - 2x - 3 = 0\). Two numbers multiply to -3 and add to -2: they are -3 and 1, giving \((x - 3)(x + 1) = 0\).
   - ask: First root, x =  [box=3, NO label]
   - ask: Second root, x =  [box=-1, NO label]
   - intro: Now each x gets its y from the line \(x + 1\).
   - ask: At x = 3: y =  [box=4, NO label]
   - ask: At x = -1: y =  [box=0, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Check: 3^2 - 3 - 2 =  [box=4, NO label]

## silver[4] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 3x\) and \(y = x^2 + 2\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(3x = x^2 + 2\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-3, label:'x']
   - ask: and the constant becomes  [box=2, NO label]
   - intro: So \(x^2 - 3x + 2 = 0\). Two numbers multiply to 2 and add to -3: they are -1 and -2, giving \((x - 1)(x - 2) = 0\).
   - ask: First root, x =  [box=1, NO label]
   - ask: Second root, x =  [box=2, NO label]
   - intro: Now each x gets its y from the line \(3x\).
   - ask: At x = 1: y =  [box=3, NO label]
   - ask: At x = 2: y =  [box=6, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Check: 1^2 + 2 =  [box=3, NO label]

## silver[5] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = x\) and \(y = x^2 - 4x + 4\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(x = x^2 - 4x + 4\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-5, label:'x']
   - ask: and the constant becomes  [box=4, NO label]
   - intro: So \(x^2 - 5x + 4 = 0\). Two numbers multiply to 4 and add to -5: they are -1 and -4, giving \((x - 1)(x - 4) = 0\).
   - ask: First root, x =  [box=1, NO label]
   - ask: Second root, x =  [box=4, NO label]
   - intro: Now each x gets its y from the line \(x\).
   - ask: At x = 1: y =  [box=1, NO label]
   - ask: At x = 4: y =  [box=4, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Check: 1^2 - 4 + 4 =  [box=1, NO label]

## silver[6] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 5 - 2x\) and \(y = x^2 - 3\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(5 - 2x = x^2 - 3\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=2, label:'x']
   - ask: and the constant becomes  [box=-8, NO label]
   - intro: So \(x^2 + 2x - 8 = 0\). Two numbers multiply to -8 and add to 2: they are -2 and 4, giving \((x - 2)(x + 4) = 0\).
   - ask: First root, x =  [box=2, NO label]
   - ask: Second root, x =  [box=-4, NO label]
   - intro: Now each x gets its y from the line \(5 - 2x\).
   - ask: At x = 2: y =  [box=1, NO label]
   - ask: At x = -4: y =  [box=13, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Check: 2^2 - 3 =  [box=1, NO label]

## gold[0] (input: two_solutions, main-box unit: (none))
Q: xyOx²+y²=10y=2x+1??Diagram not drawn accuratelySolve \(y = 2x + 1\) and \(x^2 + y^2 = 10\). Give the two x-values.
   - intro: Substitute \(y = 2x + 1\) into the circle: \(x^2 + (2x + 1)^2 = 10\). Expand \((2x + 1)^2 = 4x^2 + 4x + 1\).
   - ask: The middle term, 2 × 2x × 1, is  [box=4, label:'x']
   - intro: So \(x^2 + 4x^2 + 4x + 1 = 10\).
   - ask: Collect the x² terms: 1 + 4 =  [box=5, label:'x²']
   - intro: That gives \(5x^2 + 4x + 1 = 10\). Take 10 across.
   - ask: The constant, 1 − 10, becomes  [box=-9, NO label]
   - intro: So \(5x^2 + 4x - 9 = 0\). This factorises as \((5x + 9)(x - 1) = 0\).
   - ask: From x − 1 = 0, the first root is x =  [box=1, NO label]
   - intro: From \(5x + 9 = 0\), \(5x = -9\).
   - ask: So the second root is x =  [box=-1.8, NO label]
   - intro: Each x gets its y from \(y = 2x + 1\).
   - ask: At x = 1: y =  [box=3, NO label]
   - ask: At x = −1.8: y =  [box=-2.6, NO label]
   - ask: Check (1, 3) in the circle: 1² + 3² =  [box=10, NO label]

## gold[1] (input: two_solutions, main-box unit: (none))
Q: xyOx²+y²=13x+y=5??Diagram not drawn accuratelySolve \(x + y = 5\) and \(x^2 + y^2 = 13\). Give the two x-values.
   - intro: Rearrange the line: \(x + y = 5\) becomes \(y = 5 - x\). Substitute into \(x^2 + y^2 = 13\): \(x^2 + (5 - x)^2 = 13\). Expand \((5 - x)^2 = 25 - 10x + x^2\).
   - ask: The middle term, 2 × 5 × (−1), is  [box=-10, label:'x']
   - intro: So \(x^2 + 25 - 10x + x^2 = 13\).
   - ask: Collect the two x² terms: 1 + 1 =  [box=2, label:'x²']
   - ask: The constant, 25 − 13, becomes  [box=12, NO label]
   - intro: So \(2x^2 - 10x + 12 = 0\). Divide every term by 2 to get \(x^2 - 5x + 6 = 0\), which factorises as \((x - 2)(x - 3) = 0\).
   - ask: First root, x =  [box=2, NO label]
   - ask: Second root, x =  [box=3, NO label]
   - intro: Each x gets its y from the line.
   - ask: At x = 2: y =  [box=3, NO label]
   - ask: At x = 3: y =  [box=2, NO label]
   - ask: Check (2, 3) in the circle: 2² + 3² =  [box=13, NO label]

## gold[2] (input: two_solutions, main-box unit: (none))
Q: xyOx²+y²=25y=x−1??Diagram not drawn accuratelySolve \(y = x - 1\) and \(x^2 + y^2 = 25\). Give the two x-values.
   - intro: Substitute \(y = x - 1\) into the circle: \(x^2 + (x - 1)^2 = 25\). Expand \((x - 1)^2 = x^2 - 2x + 1\).
   - ask: The middle term, 2 × x × (−1), is  [box=-2, label:'x']
   - intro: So \(x^2 + x^2 - 2x + 1 = 25\).
   - ask: Collect the two x² terms: 1 + 1 =  [box=2, label:'x²']
   - ask: The constant, 1 − 25, becomes  [box=-24, NO label]
   - intro: So \(2x^2 - 2x - 24 = 0\). Divide every term by 2 to get \(x^2 - x - 12 = 0\), which factorises as \((x - 4)(x + 3) = 0\).
   - ask: First root, x =  [box=4, NO label]
   - ask: Second root, x =  [box=-3, NO label]
   - intro: Each x gets its y from the line.
   - ask: At x = 4: y =  [box=3, NO label]
   - ask: At x = -3: y =  [box=-4, NO label]
   - ask: Check (4, 3) in the circle: 4² + 3² =  [box=25, NO label]

## gold[3] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 3 - x\) and \(xy = 2\). Give the two x-values.
   - intro: The second equation is \(xy = 2\). Rearrange the line to \(y = 3 - x\) and substitute: \(x(3 - x) = 2\). Expanding gives \(3x - x^2 = 2\); multiply through by \(-1\) so \(x^2\) is positive.
   - ask: Make x² positive and tidy; the x-term becomes  [box=-3, label:'x']
   - ask: and the constant becomes  [box=2, NO label]
   - intro: So \(x^2 - 3x + 2 = 0\), which factorises as \((x - 1)(x - 2) = 0\).
   - ask: First root, x =  [box=1, NO label]
   - ask: Second root, x =  [box=2, NO label]
   - intro: Each x gets its y from the line \(y = 3 - x\).
   - ask: At x = 1: y =  [box=2, NO label]
   - ask: At x = 2: y =  [box=1, NO label]
   - ask: Check x = 1, y = 2 in the product: 1 × 2 =  [box=2, NO label]

## gold[4] (input: two_solutions, main-box unit: (none))
Q: Solve \(x + y = 7\) and \(xy = 10\). Give the two x-values.
   - intro: The second equation is \(xy = 10\). Rearrange the line to \(y = 7 - x\) and substitute: \(x(7 - x) = 10\). Expanding gives \(7x - x^2 = 10\); multiply through by \(-1\) so \(x^2\) is positive.
   - ask: Make x² positive and tidy; the x-term becomes  [box=-7, label:'x']
   - ask: and the constant becomes  [box=10, NO label]
   - intro: So \(x^2 - 7x + 10 = 0\), which factorises as \((x - 2)(x - 5) = 0\).
   - ask: First root, x =  [box=2, NO label]
   - ask: Second root, x =  [box=5, NO label]
   - intro: Each x gets its y from the line \(y = 7 - x\).
   - ask: At x = 2: y =  [box=5, NO label]
   - ask: At x = 5: y =  [box=2, NO label]
   - ask: Check x = 2, y = 5 in the product: 2 × 5 =  [box=10, NO label]
