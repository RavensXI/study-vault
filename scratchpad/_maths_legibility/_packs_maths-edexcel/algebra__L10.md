# maths-edexcel / algebra / L10 - Simultaneous Equations (Quadratic)

## bronze[0] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = x + 3\) and \(y = x^2 + 1\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(y = x + 3 = y = x^2 + 1\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-1, label:'x']
   - ask: and the constant becomes  [box=-2, NO label]
   - intro: So \(x^2 - 1x - 2 = 0\). Two numbers multiply to -2 and add to -1: they are -2 and 1, giving \((x - 2)(x + 1) = 0\).
   - ask: First root, x =  [box=2, NO label]
   - ask: Second root, x =  [box=-1, NO label]
   - intro: Now each x gets its y from the line \(y = y = x + 3\).
   - ask: At x = 2: y =  [box=5, NO label]
   - ask: At x = −1: y =  [box=2, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Work out 2² + 1 =  [box=5, NO label]

## bronze[1] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = x + 6\) and \(y = x^2\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(y = x + 6 = y = x^2\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-1, label:'x']
   - ask: and the constant becomes  [box=-6, NO label]
   - intro: So \(x^2 - 1x - 6 = 0\). Two numbers multiply to -6 and add to -1: they are -3 and 2, giving \((x - 3)(x + 2) = 0\).
   - ask: First root, x =  [box=3, NO label]
   - ask: Second root, x =  [box=-2, NO label]
   - intro: Now each x gets its y from the line \(y = y = x + 6\).
   - ask: At x = 3: y =  [box=9, NO label]
   - ask: At x = −2: y =  [box=4, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Work out 3² =  [box=9, NO label]

## bronze[2] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = x + 1\) and \(y = x^2 - 2x - 3\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(y = x + 1 = y = x^2 - 2x - 3\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-3, label:'x']
   - ask: and the constant becomes  [box=-4, NO label]
   - intro: So \(x^2 - 3x - 4 = 0\). Two numbers multiply to -4 and add to -3: they are -4 and 1, giving \((x - 4)(x + 1) = 0\).
   - ask: First root, x =  [box=4, NO label]
   - ask: Second root, x =  [box=-1, NO label]
   - intro: Now each x gets its y from the line \(y = y = x + 1\).
   - ask: At x = 4: y =  [box=5, NO label]
   - ask: At x = −1: y =  [box=0, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Work out 4² − 2×4 − 3 =  [box=5, NO label]

## bronze[3] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 2x\) and \(y = x^2\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(y = 2x = y = x^2\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-2, label:'x']
   - ask: and the constant becomes  [box=0, NO label]
   - intro: So \(x^2 - 2x = 0\). Take out a common factor of x: \(x(x - 2) = 0\).
   - ask: First root, x =  [box=0, NO label]
   - ask: Second root, x =  [box=2, NO label]
   - intro: Now each x gets its y from the line \(y = y = 2x\).
   - ask: At x = 0: y =  [box=0, NO label]
   - ask: At x = 2: y =  [box=4, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Work out 0² =  [box=0, NO label]

## bronze[4] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = x\) and \(y = x^2 + 5x + 3\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(y = x = y = x^2 + 5x + 3\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=4, label:'x']
   - ask: and the constant becomes  [box=3, NO label]
   - intro: So \(x^2 + 4x + 3 = 0\). Two numbers multiply to 3 and add to 4: they are 1 and 3, giving \((x + 1)(x + 3) = 0\).
   - ask: First root, x =  [box=-1, NO label]
   - ask: Second root, x =  [box=-3, NO label]
   - intro: Now each x gets its y from the line \(y = y = x\).
   - ask: At x = −1: y =  [box=-1, NO label]
   - ask: At x = −3: y =  [box=-3, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Work out (−1)² + 5×(−1) + 3 =  [box=-1, NO label]

## bronze[5] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = x - 1\) and \(y = x^2 - 3x + 2\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(y = x - 1 = y = x^2 - 3x + 2\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-4, label:'x']
   - ask: and the constant becomes  [box=3, NO label]
   - intro: So \(x^2 - 4x + 3 = 0\). Two numbers multiply to 3 and add to -4: they are -1 and -3, giving \((x - 1)(x - 3) = 0\).
   - ask: First root, x =  [box=1, NO label]
   - ask: Second root, x =  [box=3, NO label]
   - intro: Now each x gets its y from the line \(y = y = x - 1\).
   - ask: At x = 1: y =  [box=0, NO label]
   - ask: At x = 3: y =  [box=2, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Work out 1² − 3×1 + 2 =  [box=0, NO label]

## bronze[6] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 3x\) and \(y = x^2 + 2\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(y = 3x = y = x^2 + 2\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-3, label:'x']
   - ask: and the constant becomes  [box=2, NO label]
   - intro: So \(x^2 - 3x + 2 = 0\). Two numbers multiply to 2 and add to -3: they are -1 and -2, giving \((x - 1)(x - 2) = 0\).
   - ask: First root, x =  [box=1, NO label]
   - ask: Second root, x =  [box=2, NO label]
   - intro: Now each x gets its y from the line \(y = y = 3x\).
   - ask: At x = 1: y =  [box=3, NO label]
   - ask: At x = 2: y =  [box=6, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Work out 1² + 2 =  [box=3, NO label]

## bronze[7] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = x + 4\) and \(y = x^2 + x\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(y = x + 4 = y = x^2 + x\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=0, label:'x']
   - ask: and the constant becomes  [box=-4, NO label]
   - intro: So \(x^2 - 4 = 0\). Two numbers multiply to -4 and add to 0: they are -2 and 2, giving \((x - 2)(x + 2) = 0\).
   - ask: First root, x =  [box=2, NO label]
   - ask: Second root, x =  [box=-2, NO label]
   - intro: Now each x gets its y from the line \(y = y = x + 4\).
   - ask: At x = 2: y =  [box=6, NO label]
   - ask: At x = −2: y =  [box=2, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Work out 2² + 1×2 =  [box=6, NO label]

## silver[0] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 2x + 1\) and \(y = x^2 + x - 1\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(y = 2x + 1 = y = x^2 + x - 1\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-1, label:'x']
   - ask: and the constant becomes  [box=-2, NO label]
   - intro: So \(x^2 - 1x - 2 = 0\). Two numbers multiply to -2 and add to -1: they are -2 and 1, giving \((x - 2)(x + 1) = 0\).
   - ask: First root, x =  [box=2, NO label]
   - ask: Second root, x =  [box=-1, NO label]
   - intro: Now each x gets its y from the line \(y = y = 2x + 1\).
   - ask: At x = 2: y =  [box=5, NO label]
   - ask: At x = −1: y =  [box=-1, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Work out 2² + 1×2 − 1 =  [box=5, NO label]

## silver[1] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = x + 3\) and \(y = x^2 - 2x - 1\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(y = x + 3 = y = x^2 - 2x - 1\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-3, label:'x']
   - ask: and the constant becomes  [box=-4, NO label]
   - intro: So \(x^2 - 3x - 4 = 0\). Two numbers multiply to -4 and add to -3: they are -4 and 1, giving \((x - 4)(x + 1) = 0\).
   - ask: First root, x =  [box=4, NO label]
   - ask: Second root, x =  [box=-1, NO label]
   - intro: Now each x gets its y from the line \(y = y = x + 3\).
   - ask: At x = 4: y =  [box=7, NO label]
   - ask: At x = −1: y =  [box=2, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Work out 4² − 2×4 − 1 =  [box=7, NO label]

## silver[2] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 3 - x\) and \(y = x^2 - 3\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(y = 3 - x = y = x^2 - 3\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=1, label:'x']
   - ask: and the constant becomes  [box=-6, NO label]
   - intro: So \(x^2 + 1x - 6 = 0\). Two numbers multiply to -6 and add to 1: they are -2 and 3, giving \((x - 2)(x + 3) = 0\).
   - ask: First root, x =  [box=2, NO label]
   - ask: Second root, x =  [box=-3, NO label]
   - intro: Now each x gets its y from the line \(y = y = 3 - x\).
   - ask: At x = 2: y =  [box=1, NO label]
   - ask: At x = −3: y =  [box=6, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Work out 2² − 3 =  [box=1, NO label]

## silver[3] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 2x - 3\) and \(y = x^2 - 3\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(y = 2x - 3 = y = x^2 - 3\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-2, label:'x']
   - ask: and the constant becomes  [box=0, NO label]
   - intro: So \(x^2 - 2x = 0\). Take out a common factor of x: \(x(x - 2) = 0\).
   - ask: First root, x =  [box=0, NO label]
   - ask: Second root, x =  [box=2, NO label]
   - intro: Now each x gets its y from the line \(y = y = 2x - 3\).
   - ask: At x = 0: y =  [box=-3, NO label]
   - ask: At x = 2: y =  [box=1, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Work out 0² − 3 =  [box=-3, NO label]

## silver[4] (input: two_solutions, main-box unit: (none))
Q: Solve \(x + y = 4\) and \(xy = 3\). Give the two x-values.
   - intro: Rearrange the line: \(x + y = 4\) becomes \(y = 4 - x\). Substitute into \(xy = 3\): \(x(4 - x) = 3\), which expands to \(4x - x^2 = 3\). Bring everything to one side so \(x^2\) is positive.
   - ask: The equation becomes x² − 4x +  [box=3, label:' = 0']
   - intro: So \(x^2 - 4x + 3 = 0\). Two numbers multiply to 3 and add to \(-4\): they are \(-1\) and \(-3\), giving \((x - 1)(x - 3) = 0\).
   - ask: First root, x =  [box=1, NO label]
   - ask: Second root, x =  [box=3, NO label]
   - intro: Each x gets its y from \(y = 4 - x\).
   - ask: At x = 1: y =  [box=3, NO label]
   - ask: At x = 3: y =  [box=1, NO label]
   - intro: Notice the pairs are (1, 3) and (3, 1): the same two numbers.
   - ask: Check by multiplying: 1 × 3 =  [box=3, NO label]

## silver[5] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = 5 - 2x\) and \(y = x^2 - 4x + 2\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(y = 5 - 2x = y = x^2 - 4x + 2\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=-2, label:'x']
   - ask: and the constant becomes  [box=-3, NO label]
   - intro: So \(x^2 - 2x - 3 = 0\). Two numbers multiply to -3 and add to -2: they are -3 and 1, giving \((x - 3)(x + 1) = 0\).
   - ask: First root, x =  [box=3, NO label]
   - ask: Second root, x =  [box=-1, NO label]
   - intro: Now each x gets its y from the line \(y = y = 5 - 2x\).
   - ask: At x = 3: y =  [box=-1, NO label]
   - ask: At x = −1: y =  [box=7, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Work out 3² − 4×3 + 2 =  [box=-1, NO label]

## silver[6] (input: two_solutions, main-box unit: (none))
Q: Solve \(y = x + 2\) and \(y = x^2 + x - 2\). Give the two x-values.
   - intro: Both equations give y, so set the two right sides equal: \(y = x + 2 = y = x^2 + x - 2\). Bring every term to the right so \(x^2\) stays positive.
   - ask: The x-term becomes  [box=0, label:'x']
   - ask: and the constant becomes  [box=-4, NO label]
   - intro: So \(x^2 - 4 = 0\). Two numbers multiply to -4 and add to 0: they are -2 and 2, giving \((x - 2)(x + 2) = 0\).
   - ask: First root, x =  [box=2, NO label]
   - ask: Second root, x =  [box=-2, NO label]
   - intro: Now each x gets its y from the line \(y = y = x + 2\).
   - ask: At x = 2: y =  [box=4, NO label]
   - ask: At x = −2: y =  [box=0, NO label]
   - intro: Last check: put the first pair into the curve.
   - ask: Work out 2² + 1×2 − 2 =  [box=4, NO label]

## gold[0] (input: two_solutions, main-box unit: (none))
Q: xyOx²+y²=25x+y=7??Diagram not drawn accuratelySolve \(x + y = 7\) and \(x^2 + y^2 = 25\). Give the two x-values.
   - intro: Rearrange the line: \(x + y = 7\) becomes \(y = 7 - x\). Substitute into the circle: \(x^2 + (7 - x)^2 = 25\). Expand \((7 - x)^2 = 49 - 14x + x^2\).
   - ask: The middle term, 2 × 7 × (−1), is  [box=-14, label:'x']
   - intro: So \(x^2 + 49 - 14x + x^2 = 25\).
   - ask: Collect the two x² terms: 1 + 1 =  [box=2, label:'x²']
   - intro: That gives \(2x^2 - 14x + 49 = 25\). Take 25 across.
   - ask: The constant, 49 − 25, becomes  [box=24, NO label]
   - intro: So \(2x^2 - 14x + 24 = 0\). Divide every term by 2 to get \(x^2 - 7x + 12 = 0\), which factorises as \((x - 3)(x - 4) = 0\).
   - ask: First root, x =  [box=3, NO label]
   - ask: Second root, x =  [box=4, NO label]
   - intro: Each x gets its y from \(y = 7 - x\).
   - ask: At x = 3: y =  [box=4, NO label]
   - ask: At x = 4: y =  [box=3, NO label]
   - ask: Check (3, 4) in the circle: 3² + 4² =  [box=25, NO label]

## gold[1] (input: two_solutions, main-box unit: (none))
Q: xyOx²+y²=10y=2x+1??Diagram not drawn accuratelySolve \(y = 2x + 1\) and \(x^2 + y^2 = 10\). Give the two x-values.
   - intro: Substitute \(y = 2x + 1\) into the circle: \(x^2 + (2x + 1)^2 = 10\). Expand \((2x + 1)^2 = 4x^2 + 4x + 1\).
   - ask: The middle term, 2 × 2x × 1, is  [box=4, label:'x']
   - intro: So \(x^2 + 4x^2 + 4x + 1 = 10\).
   - ask: Collect the x² terms: 1 + 4 =  [box=5, label:'x²']
   - intro: That gives \(5x^2 + 4x + 1 = 10\). Take 10 across.
   - ask: The constant, 1 − 10, becomes  [box=-9, NO label]
   - intro: So \(5x^2 + 4x - 9 = 0\). This factorises as \((5x + 9)(x - 1) = 0\).
   - ask: From x − 1 = 0, the first root is x =  [box=1, NO label]
   - intro: From \(5x + 9 = 0\), \(5x = -9\).
   - ask: So the second root is x =  [box=-1.8, NO label]
   - intro: Each x gets its y from \(y = 2x + 1\).
   - ask: At x = 1: y =  [box=3, NO label]
   - ask: At x = −1.8: y =  [box=-2.6, NO label]
   - ask: Check (1, 3) in the circle: 1² + 3² =  [box=10, NO label]

## gold[2] (input: two_solutions, main-box unit: (none))
Q: xyOx²+y²=13x−y=1??Diagram not drawn accuratelySolve \(x - y = 1\) and \(x^2 + y^2 = 13\). Give the two x-values.
   - intro: Rearrange the line: \(x - y = 1\) becomes \(y = x - 1\). Substitute: \(x^2 + (x - 1)^2 = 13\). Expand \((x - 1)^2 = x^2 - 2x + 1\).
   - ask: The middle term, 2 × x × (−1), is  [box=-2, label:'x']
   - intro: So \(x^2 + x^2 - 2x + 1 = 13\).
   - ask: Collect the two x² terms: 1 + 1 =  [box=2, label:'x²']
   - intro: That gives \(2x^2 - 2x + 1 = 13\). Take 13 across.
   - ask: The constant, 1 − 13, becomes  [box=-12, NO label]
   - intro: So \(2x^2 - 2x - 12 = 0\). Divide by 2 to get \(x^2 - x - 6 = 0\), which factorises as \((x - 3)(x + 2) = 0\).
   - ask: First root, x =  [box=3, NO label]
   - ask: Second root, x =  [box=-2, NO label]
   - intro: Each x gets its y from \(y = x - 1\).
   - ask: At x = 3: y =  [box=2, NO label]
   - ask: At x = −2: y =  [box=-3, NO label]
   - ask: Check (3, 2) in the circle: 3² + 2² =  [box=13, NO label]

## gold[3] (input: two_solutions, main-box unit: (none))
Q: xyOx²+y²=20y=x+2??Diagram not drawn accuratelySolve \(y = x + 2\) and \(x^2 + y^2 = 20\). Give the two x-values.
   - intro: Substitute \(y = x + 2\) into the circle: \(x^2 + (x + 2)^2 = 20\). Expand \((x + 2)^2 = x^2 + 4x + 4\).
   - ask: The middle term, 2 × x × 2, is  [box=4, label:'x']
   - intro: So \(x^2 + x^2 + 4x + 4 = 20\).
   - ask: Collect the two x² terms: 1 + 1 =  [box=2, label:'x²']
   - intro: That gives \(2x^2 + 4x + 4 = 20\). Take 20 across.
   - ask: The constant, 4 − 20, becomes  [box=-16, NO label]
   - intro: So \(2x^2 + 4x - 16 = 0\). Divide by 2 to get \(x^2 + 2x - 8 = 0\), which factorises as \((x - 2)(x + 4) = 0\).
   - ask: First root, x =  [box=2, NO label]
   - ask: Second root, x =  [box=-4, NO label]
   - intro: Each x gets its y from \(y = x + 2\).
   - ask: At x = 2: y =  [box=4, NO label]
   - ask: At x = −4: y =  [box=-2, NO label]
   - ask: Check (2, 4) in the circle: 2² + 4² =  [box=20, NO label]

## gold[4] (input: two_solutions, main-box unit: (none))
Q: Solve \(x + y = 5\) and \(x^2 - y = 7\). Give the two x-values.
   - intro: Rearrange the line: \(x + y = 5\) becomes \(y = 5 - x\). Substitute into \(x^2 - y = 7\): \(x^2 - (5 - x) = 7\). Removing the bracket gives \(x^2 - 5 + x = 7\).
   - ask: Bring −5 and −7 together for the constant:  [box=-12, NO label]
   - intro: So \(x^2 + x - 12 = 0\). Two numbers multiply to \(-12\) and add to \(+1\): they are \(+4\) and \(-3\), giving \((x + 4)(x - 3) = 0\).
   - ask: First root, x =  [box=3, NO label]
   - ask: Second root, x =  [box=-4, NO label]
   - intro: Each x gets its y from \(y = 5 - x\).
   - ask: At x = 3: y =  [box=2, NO label]
   - ask: At x = −4: y =  [box=9, NO label]
   - intro: Check the first pair in the second equation.
   - ask: Put x = 3, y = 2 into x² − y: 3² − 2 =  [box=7, NO label]
