# maths-edexcel / graphs / L07 - Graph Transformations

## bronze[0] (input: single_value, main-box unit: (none))
Q: The point \((2, 5)\) lies on \(y = f(x)\). Find the \(y\)-coordinate of its image on \(y = f(x) + 4\).
   - intro: The +4 is OUTSIDE the bracket, so it changes the y-coordinate only. The x-coordinate stays at 2.
   - ask: The graph moves UP. How many units up?  [box=4, NO label]
   - intro: So add that to the y-coordinate.
   - ask: New y = 5 + 4 =  [box=9, NO label]
   - ask: Check how far y rose: 9 − 5 =  [box=4, NO label]

## bronze[1] (input: single_value, main-box unit: (none))
Q: The point \((3, 1)\) lies on \(y = f(x)\). Find the \(y\)-coordinate of its image on \(y = f(x) - 2\).
   - intro: The −2 is OUTSIDE the bracket, so it changes the y-coordinate only. x stays at 3.
   - ask: The graph moves DOWN. By how many units?  [box=2, NO label]
   - intro: So subtract that from the y-coordinate.
   - ask: New y = 1 − 2 =  [box=-1, NO label]
   - ask: Check the drop: 1 − (−1) =  [box=2, NO label]

## bronze[2] (input: multiple_choice, main-box unit: (none))
Q: \(y = f(x) + 3\) is a translation. Which direction does the graph move?

## bronze[3] (input: multiple_choice, main-box unit: (none))
Q: \(y = f(x + 4)\) is a translation. Which direction does the graph move?

## bronze[4] (input: single_value, main-box unit: (none))
Q: The point \((6, 2)\) lies on \(y = f(x)\). Find the \(x\)-coordinate of its image on \(y = f(x + 3)\).
   - intro: The +3 is INSIDE the bracket, so it changes the x-coordinate, and inside does the OPPOSITE: it moves LEFT. y stays at 2.
   - ask: Moving left means we subtract. By how many?  [box=3, NO label]
   - intro: Take that off the x-coordinate.
   - ask: New x = 6 − 3 =  [box=3, NO label]
   - ask: Check: y is untouched, so how far did y move?  [box=0, NO label]

## bronze[5] (input: multiple_choice, main-box unit: (none))
Q: \(y = -f(x)\) is a reflection. In which axis?

## bronze[6] (input: multiple_choice, main-box unit: (none))
Q: \(y = f(-x)\) is a reflection. In which axis?

## bronze[7] (input: single_value, main-box unit: (none))
Q: The point \((5, 8)\) lies on \(y = f(x)\). Find the \(y\)-coordinate of its image on \(y = -f(x)\).
   - intro: The minus is OUTSIDE the bracket, so −f(x) reflects the graph in the x-axis: every y-value flips sign. x stays at 5.
   - ask: Before flipping, the y-coordinate is  [box=8, NO label]
   - intro: Reflecting multiplies that by −1.
   - ask: New y = 8 × (−1) =  [box=-8, NO label]
   - ask: Check the two heights cancel: 8 + (−8) =  [box=0, NO label]

## silver[0] (input: single_value, main-box unit: (none))
Q: The point \((-1, 4)\) lies on \(y = f(x)\). Find the \(x\)-coordinate of its image on \(y = f(-x)\).
   - intro: The minus is INSIDE the bracket, so f(−x) reflects the graph in the y-axis: every x-value flips sign. y stays at 4.
   - ask: Before flipping, the x-coordinate is  [box=-1, NO label]
   - intro: Reflecting multiplies that by −1.
   - ask: New x = (−1) × (−1) =  [box=1, NO label]
   - ask: Check the two x-values cancel: (−1) + 1 =  [box=0, NO label]

## silver[1] (input: single_value, main-box unit: (none))
Q: The maximum point of \(y = f(x)\) is \((3, 7)\). Find the \(y\)-coordinate of the maximum of \(y = f(x) - 5\).
   - intro: The maximum point moves with the curve. The −5 is OUTSIDE, so it lowers the y-coordinate; x stays at 3.
   - ask: The graph moves DOWN by how many?  [box=5, NO label]
   - intro: Take that off the maximum's y-coordinate.
   - ask: New y = 7 − 5 =  [box=2, NO label]
   - ask: Check the drop: 7 − 2 =  [box=5, NO label]

## silver[2] (input: single_value, main-box unit: (none))
Q: The minimum point of \(y = f(x)\) is \((2, -3)\). Find the \(x\)-coordinate of the minimum of \(y = f(x - 4)\).
   - intro: The minimum moves with the curve. The −4 is INSIDE, and inside does the OPPOSITE, so the graph moves RIGHT; y stays at −3.
   - ask: Moving right means we add. Add how many?  [box=4, NO label]
   - intro: Add that to the x-coordinate.
   - ask: New x = 2 + 4 =  [box=6, NO label]
   - ask: Check how far right it moved: 6 − 2 =  [box=4, NO label]

## silver[3] (input: single_value, main-box unit: (none))
Q: The curve \(y = x^2\) is transformed to \(y = (x - 3)^2 + 1\) by a translation. Give the \(x\)-component of the translation vector.
   - intro: A translation vector is written as (x-shift, y-shift). Find each part from the equation.
   - ask: The +1 outside the bracket shifts the curve up. That is the y-component:  [box=1, NO label]
   - intro: Now the horizontal part, from inside the bracket.
   - ask: Inside is (x − 3). Inside does the opposite, so the curve moves RIGHT by  [box=3, NO label]
   - ask: Check: a point at x = 0 on y = x² lands at x =  [box=3, NO label]

## silver[4] (input: single_value, main-box unit: (none))
Q: The graph \(y = f(x)\) passes through \((0, 5)\). What point does \(y = f(x + 2) - 1\) pass through? Give the \(y\)-coordinate.
   - intro: Two moves. The +2 inside changes x; the −1 outside changes y. We want the y-coordinate, which the outside part controls.
   - ask: The x-part first: +2 inside moves LEFT, so new x = 0 − 2 =  [box=-2, NO label]
   - intro: Now the y-coordinate, from the −1 outside.
   - ask: New y = 5 − 1 =  [box=4, NO label]
   - ask: Check the drop in y: 5 − 4 =  [box=1, NO label]

## silver[5] (input: single_value, main-box unit: (none))
Q: The point \((4, -2)\) lies on \(y = f(x)\). It is reflected in the \(y\)-axis to give \(y = f(-x)\). What is the new \(x\)-coordinate?
   - intro: Reflecting in the y-axis flips the sign of every x-coordinate. y stays at −2.
   - ask: The x-coordinate before reflecting is  [box=4, NO label]
   - intro: Multiply that by −1.
   - ask: New x = 4 × (−1) =  [box=-4, NO label]
   - ask: Check the two x-values cancel: 4 + (−4) =  [box=0, NO label]

## silver[6] (input: single_value, main-box unit: (none))
Q: The point \((-3, 6)\) lies on \(y = f(x)\). Find the \(y\)-coordinate of its image on \(y = -f(x)\).
   - intro: −f(x) reflects in the x-axis, flipping the sign of every y-coordinate. x stays at −3.
   - ask: The y-coordinate before reflecting is  [box=6, NO label]
   - intro: Multiply that by −1.
   - ask: New y = 6 × (−1) =  [box=-6, NO label]
   - ask: Check the two y-values cancel: 6 + (−6) =  [box=0, NO label]

## gold[0] (input: single_value, main-box unit: (none))
Q: The curve \(y = x^2\) is transformed to \(y = (x + 1)^2 - 4\). State the coordinates of the vertex of the new curve. Give the \(y\)-coordinate.
   - intro: Start from the vertex of y = x², which sits at (0, 0). Apply the two moves to it.
   - ask: Inside is (x + 1). Inside does the opposite, so the vertex moves LEFT by 1: new x = 0 − 1 =  [box=-1, NO label]
   - intro: Now the vertical move, from the −4 outside. We want this y-coordinate.
   - ask: New y = 0 − 4 =  [box=-4, NO label]
   - ask: Check the drop: 0 − (−4) =  [box=4, NO label]

## gold[1] (input: multiple_choice, main-box unit: (none))
Q: The graph of \(y = \sin x\) is reflected in the \(x\)-axis. Write the equation of the new graph. Which of these is correct?

## gold[2] (input: single_value, main-box unit: (none))
Q: Two transformations are applied: first \(y = f(x)\) becomes \(y = -f(x)\), then that becomes \(y = -f(x) + 3\). The point \((2, 5)\) is on the original. Find the final \(y\)-coordinate.
   - intro: Two steps on the y-coordinate. First reflect, then add 3. x stays at 2.
   - ask: Reflect: −f(x) flips the sign of y, so 5 becomes  [box=-5, NO label]
   - intro: Now the +3 lifts that reflected y.
   - ask: New y = (−5) + 3 =  [box=-2, NO label]
   - ask: Check the lift: (−2) − (−5) =  [box=3, NO label]

## gold[3] (input: multiple_choice, main-box unit: (none))
Q: The point \((a, b)\) on \(y = f(x)\) maps to \((-a, -b)\). Which two transformations produce this?

## gold[4] (input: multiple_choice, main-box unit: (none))
Q: The curve \(y = f(x)\) has a root at \(x = 5\). After the transformation \(y = f(x + 2) + 3\), does the curve still pass through the \(x\)-axis at \(x = 3\)?
