{
 "key": "number-L05",
 "board": "maths-ocr",
 "problems_fixed": [],
 "issues_resolved": 0,
 "misconceptions_enriched": "All misconceptions rewritten with derived numeric `expect` values (bank previously had string `check` matchers and no expect, so honest diagnosis never fired). 0-2 real, committed-error expects per problem; none equal the correct answer.",
 "style_repairs": "Replaced em dashes with colons in 6 preserved worked_examples step labels (no-em-dash style law).",
 "opener_concept": "Sale price: a 25% off tag on a £40 coat. Student finds the £10 saving (a quarter of 40) and the £30 till price by common sense; reveal names it as a percentage decrease and the ×0.75 multiplier shortcut. Inline SVG price tag shown (currentColor, soft fill).",
 "figures_added": [
  {
   "tier": "opener",
   "index": 0,
   "kind": "svg",
   "what": "Sale price tag reading '25% OFF / £40 coat', matching the opener numbers; theme-safe currentColor text + soft blue fill, no external refs."
  }
 ],
 "notes": "number-L05 Percentages, maths-ocr. Full guided stack added: opener, 3 teach walks (>=4 boxes each, not in bank), tier_guides (bronze/silver/gold within 115-word budget), tier_descriptions, per-problem hint + guided_steps with phase:'substitute' completion boundary (setup pre-worked, solve-through + check live). method_card trimmed to slim reference (<=140 words). Bank maths was clean on fresh-solve (contrast Edexcel L09/L10). Independent evaluator re-checked 79 boxes: all consistent (one flagged item is the deliberate round-to-nearest-£ on 2000x1.03^3=2185.45->2185). Validator PASS; live PATCH 204."
}