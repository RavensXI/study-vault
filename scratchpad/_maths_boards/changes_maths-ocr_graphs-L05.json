{
  "key": "graphs-L05",
  "board": "maths-ocr",
  "lesson_id": "acf8619c-92bc-4778-b29c-dd0cb973f59c",
  "problems_fixed": [],
  "bank_correctness": "All 20 problems (8 bronze / 7 silver / 5 gold) fresh-solved from display text; every stored solution was already correct. No duplicate answers within any tier, all non-calculator answers clean. No arithmetic or pose repairs required.",
  "misconceptions_rebuilt": "All 20 misconceptions rewritten: the pre-existing messages were solution restatements with no expect key (would fail the validator). Each now carries a derived expect equal to the exact wrong answer its named error produces, plus an honest-diagnosis message. Examples: bronze[0] expect 9 (3x3), bronze[3] expect 6 (2x3), silver[2] expect 0.6 (5=3/k), gold[0] expect 1296 ((2x3)^4), gold[1] expect 9 (stops at x^2=9), gold[2] expect 20 (drops the +2).",
  "figures_added": [
    {"tier": "bronze", "index": 5, "kind": "chart", "what": "Reciprocal y = 6/x, both branches (Q1 and Q3), showing both axes as asymptotes for the 'which type has asymptotes at x=0 and y=0' MC."},
    {"tier": "silver", "index": 4, "kind": "chart", "what": "Exponential decay y = 0.5^x falling toward 0 as x increases, for the 'which equation decays toward 0' MC."},
    {"tier": "gold", "index": 1, "kind": "chart", "what": "Cubic y = x^3 - 9x crossing the x-axis at -3, 0, 3, for the positive-root question."}
  ],
  "style_fixes": [
    "worked_examples labels 'Step 1 — Substitute' / 'Step 1 — Calculate' had em dashes; changed to colons.",
    "method_card trimmed from 5 steps to 4 (validator cap) and content re-slimmed under 140 words; example kept."
  ],
  "added_structure": {
    "guided.opener": "Magic-penny doubling (1p, 2p, 4p, ...) -> y = 2^x reveal; 2 interactive boxes + say-only reveal linking to cubes and sharing.",
    "guided.teach": "bronze y=x^3 points (2->8, 4->64); silver y=x^3+2 at x=-2 (-6) with x=0 check; gold recover a,b from (1,12),(2,48) -> a=3,b=4.",
    "guided_steps": "Micro-step walks on all 18 non-MC problems, each with a phase:'substitute' completion boundary (set-up pre-worked, solve-through + check live) and a final verifying check step.",
    "tier_guides": "bronze/silver/gold reference cards with stepped examples (x=4->64; x=-2 on x^3-4 -> -12; a.b^x through (1,10),(2,50) -> b=5)."
  },
  "opener_concept": "A magic penny that doubles every day; counting days is exponential growth y = 2^x.",
  "preservation": "related_videos ([]) and topic_links ({prerequisites: []}) preserved byte-for-byte vs the maths-ocr pre-dump. Tier sizes and input types unchanged.",
  "issues_resolved": 20,
  "notes": "Validator PASS; PATCH 204 with round-trip equality confirmed. Independent verify script: every guided box lands on the stored solution, every expect differs from and reproduces its error, all chart points satisfy their equations."
}
