{
 "key": "algebra-L02",
 "board": "maths-aqa",
 "id": "b749e688-9faa-49ba-ae68-08f8abdc7496",
 "problems_fixed": [],
 "issues_resolved": 0,
 "bank_audit": "Fresh-solved all 20 problems (8 bronze, 7 silver, 5 gold) from their displays. Every stored solution correct: MC correct index 0 matches the true symbolic expansion (verified with sympy); gold[3] single_value a=4 confirmed (b=12). No wrong answers, no degenerate/messy non-calculator problems, no in-tier duplicate solutions (MC exempt). No maths repairs needed.",
 "opener_concept": "Area (split-rectangle) model: a 3-tall rectangle split into widths 10 and 4. Student computes each block area (3x10=30, 3x4=12), adds to 42, sees it equals 3x14. Reveal swaps 10 for x, naming 3(x+4)=3x+12 as expanding. Inline theme-safe SVG.",
 "added": {
  "hints": "All 20 problems (none had hints before).",
  "tier_descriptions": "bronze/silver/gold_description added.",
  "misconceptions": "Rebuilt with honest expect values = original option index of the matching distractor (MC) or the wrong numeric value (gold[3]: expect 12=gave b, expect 0=dropped middle terms). 2 per problem, each derived by committing the specific error.",
  "guided_steps": "silver[0] and gold[0] (FOIL walks, enable completion + Stuck lifeline at the two double-bracket tiers) and gold[3] (required, single_value: full expand-subtract-read-off walk with phase:substitute boundary).",
  "tier_guides": "bronze/silver/gold reference cards, each with worked example incl. check + answer step.",
  "guided.opener": "area-model hook (SVG).",
  "guided.teach": "one non-bank walk per tier: 4(x+5), (x+2)(x+5), (x+6)^2."
 },
 "figures_added": [
  {
   "tier": "opener",
   "index": 0,
   "kind": "svg",
   "what": "Split-rectangle area model (3 tall, widths 10 and 4) making the distributive law concrete; theme-safe currentColor + soft fills."
  }
 ],
 "method_card": "Trimmed content to 87 words (was over budget and contained an em dash); kept 4 steps. No em dashes anywhere.",
 "opener_touched": true,
 "preservation": "related_videos, topic_links, worked_examples preserved byte-for-byte. input_types, options, calculator flags, solutions all unchanged.",
 "validator": "PASS (structure, style, budgets). Independent sympy re-solve of every option and every guided box: ALL PASS.",
 "notes": "Lesson is MC-heavy (19 MC + 1 single_value); MC problems keep their input type per spec. Completion fires on silver and gold (rich FOIL first-problems); bronze single-bracket walks would be filler so left as clean MC."
}