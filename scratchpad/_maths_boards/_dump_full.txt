======================================================================
TIER BRONZE   desc: Read a gradient or intercept straight from y = mx + c, or find a gradient from two friendly points.
------------------------------------------------------------
[bronze][0] input_type=single_value calc=False
 display: Find the gradient of the line through \((0, 2)\) and \((3, 8)\).
 solutions: [2]
 answer: null
 hint: Rise over run: the change in y divided by the change in x.
   MC[0] expect=0.5 pattern=rise_run_inverted
        msg=Rise = 8 − 2 = 6, Run = 3 − 0 = 3. Gradient = 6 ÷ 3 = 2. Run over rise gives 0.5, which is upside down; the vertical change goes on top.
   step[0] SAY: Gradient means rise over run: how far up for how far across. Start with the rise, the change in y.
   step[1] pre='Rise = 8 − 2 = ' post='' ANS=6
        hint=Take the first y from the second y.
   step[2] pre='Run = 3 − 0 = ' post='' ANS=3
        hint=Take the first x from the second x. Subtracting a negative adds.
   step[3] pre='Gradient = rise ÷ run = 6 ÷ 3 = ' post='' ANS=2 PHASE=substitute
        hint=Divide the rise by the run.
   step[4] pre='Check: gradient × run = 2 × 3 = ' post='' ANS=6 PHASE=substitute
        hint=Multiply back. It should return the rise.
        done=It gives the rise back, so the gradient is right.
------------------------------------------------------------
[bronze][1] input_type=single_value calc=False
 display: Find the gradient of the line through \((1, 4)\) and \((5, 16)\).
 solutions: [3]
 answer: null
 hint: Work out the rise and the run, then divide rise by run.
   MC[0] expect=12 pattern=forgot_to_divide
        msg=Rise = 16 − 4 = 12 is only the top of the fraction. Divide by the run: 12 ÷ 4 = 3.
   step[0] SAY: Gradient means rise over run: how far up for how far across. Start with the rise, the change in y.
   step[1] pre='Rise = 16 − 4 = ' post='' ANS=12
        hint=Take the first y from the second y.
   step[2] pre='Run = 5 − 1 = ' post='' ANS=4
        hint=Take the first x from the second x. Subtracting a negative adds.
   step[3] pre='Gradient = rise ÷ run = 12 ÷ 4 = ' post='' ANS=3 PHASE=substitute
        hint=Divide the rise by the run.
   step[4] pre='Check: gradient × run = 3 × 4 = ' post='' ANS=12 PHASE=substitute
        hint=Multiply back. It should return the rise.
        done=It gives the rise back, so the gradient is right.
------------------------------------------------------------
[bronze][2] input_type=single_value calc=False
 display: A line has equation \(y = 3x + 5\). What is the y-intercept?
 solutions: [5]
 answer: null
 hint: The y-intercept is c, the number with no x next to it.
   MC[0] expect=3 pattern=confused_m_and_c
        msg=In y = mx + c the y-intercept is c, the number on its own. Here c = 5. The 3 is the gradient.
   step[0] SAY: In y = mx + c, the gradient m sits in front of x and the intercept c stands alone.
   step[1] pre='The number in front of x is the gradient m = ' post='' ANS=3
        hint=It is written just before the x.
   step[2] pre='The number on its own is the intercept c = ' post='' ANS=5
        hint=It is the term with no x.
   step[3] pre='The y-intercept is c, so the answer is ' post='' ANS=5 PHASE=substitute
        hint=The intercept is c, not m.
   step[4] pre='Check at x = 0: y = m × 0 + c = ' post='' ANS=5 PHASE=substitute
        hint=Anything times 0 is 0, leaving just c.
        done=At x = 0 the line sits at c, which is the y-intercept.
------------------------------------------------------------
[bronze][3] input_type=single_value calc=False
 display: A line has equation \(y = 4x - 3\). What is the gradient?
 solutions: [4]
 answer: null
 hint: The gradient is m, the number multiplying x.
   MC[0] expect=-3 pattern=confused_m_and_c
        msg=The gradient is m, the number in front of x. Here m = 4. The −3 is the y-intercept.
   step[0] SAY: In y = mx + c, the gradient m is the number multiplying x.
   step[1] pre='The number on its own is the intercept c = ' post='' ANS=-3
        hint=It is the term with no x.
   step[2] pre='At x = 0: y = ' post='' ANS=-3
        hint=That is just c.
   step[3] pre='At x = 1: y = 4 × 1 + (−3) = ' post='' ANS=1 PHASE=substitute
        hint=Work out m times 1, then add c.
   step[4] pre='The gradient is the rise for one step across: 1 − (−3) = ' post='' ANS=4 PHASE=substitute
        hint=Subtract the two y-values.
        done=y rises by m for each step, so the gradient is m.
------------------------------------------------------------
[bronze][4] input_type=single_value calc=False
 display: Find the gradient of the line through \((2, 1)\) and \((6, 5)\).
 solutions: [1]
 answer: null
 hint: Work out the rise and the run, then divide.
   MC[0] expect=4 pattern=forgot_to_divide
        msg=Rise = 5 − 1 = 4 is only the top. Divide by the run: 4 ÷ 4 = 1.
   step[0] SAY: Gradient means rise over run: how far up for how far across. Start with the rise, the change in y.
   step[1] pre='Rise = 5 − 1 = ' post='' ANS=4
        hint=Take the first y from the second y.
   step[2] pre='Run = 6 − 2 = ' post='' ANS=4
        hint=Take the first x from the second x. Subtracting a negative adds.
   step[3] pre='Gradient = rise ÷ run = 4 ÷ 4 = ' post='' ANS=1 PHASE=substitute
        hint=Divide the rise by the run.
   step[4] pre='Check: gradient × run = 1 × 4 = ' post='' ANS=4 PHASE=substitute
        hint=Multiply back. It should return the rise.
        done=It gives the rise back, so the gradient is right.
------------------------------------------------------------
[bronze][5] input_type=single_value calc=False
 display: The line \(y = 5x + 1\). What is the value of \(y\) when \(x = 3\)?
 solutions: [16]
 answer: null
 hint: Multiply 5 by 3 first, then add 1.
   MC[0] expect=20 pattern=order_of_operations
        msg=Multiply before adding: 5 × 3 = 15, then 15 + 1 = 16. Doing 3 + 1 first, then × 5, gives 20.
   step[0] SAY: Substitute the x-value into y = mx + c, doing the multiplication first.
   step[1] pre='The gradient part: 5 × 3 = ' post='' ANS=15
        hint=Multiply m by x before touching c.
   step[2] pre='Now add the intercept: 15 + 1 = ' post='' ANS=16 PHASE=substitute
        hint=Add c to what you just found.
   step[3] pre='Check the point (3, 16) fits: 5 × 3 + 1 = ' post='' ANS=16 PHASE=substitute
        hint=Work the equation once more.
        done=The point fits the line, so y is correct.
------------------------------------------------------------
[bronze][6] input_type=single_value calc=False
 display: The graph shows a straight line. What is the value of \(y\) when \(x = 3\)?
 solutions: [7]
 answer: null
 hint: Trace up from x = 3 to the line, then across to the y-axis.
 CHART: {"data": {"labels": [0, 1, 2, 3, 4, 5], "datasets": [{"data": [1, 3, 5, 7, 9, 11], "fill": false, "borderColor": "#3b82f6", "pointRadius": 5, "backgroundColor": "rgba(59,130,246,0.1)", "pointBackgroundColor": "#3b82f6"}]}, "type": "line", "options": {"scales": {"x": {"grid": {"color": "rgba(0,0,0,0.05)"}, "title": {"text": "x", "display": true}}, "y": {"grid": {"color": "rgba(0,0,0,0.08)"}, "ticks": {"stepSize": 2}, "title": {"text": "y", "display": true}, "beginAtZero": true}}}}
   MC[0] expect=2 pattern=value_vs_gradient
        msg=The line climbs 2 for each step across, but the question wants the height at x = 3, which is 7. The 2 is the gradient, not the value asked for.
   step[0] SAY: Read the height of the line at the x-value the question asks for.
   step[1] pre='At x = 0 the line is at y = ' post='' ANS=1
        hint=Where does it cross the y-axis?
   step[2] pre='At x = 1 the line is at y = ' post='' ANS=3
        hint=Trace up from x = 1 to the line.
   step[3] pre='Each step across adds (3 − 1) = ' post='' ANS=2
        hint=The jump in y for one step in x.
   step[4] pre='From x = 1 (y = 3), two more steps to x = 3: 3 + 2 × 2 = ' post='' ANS=7 PHASE=substitute
        hint=Add two more steps of 2.
   step[5] pre='Check on the graph at x = 3: y = ' post='' ANS=7 PHASE=substitute
        hint=Read straight up to the line.
        done=Reading and calculating agree, so y = 7.
------------------------------------------------------------
[bronze][7] input_type=single_value calc=False
 display: The graph shows a straight line. What is the gradient?
 solutions: [-2]
 answer: null
 hint: The line falls, so the gradient is negative; use rise over run.
 CHART: {"data": {"labels": [0, 1, 2, 3, 4, 5], "datasets": [{"data": [10, 8, 6, 4, 2, 0], "fill": false, "borderColor": "#ef4444", "pointRadius": 5, "backgroundColor": "rgba(239,68,68,0.1)", "pointBackgroundColor": "#ef4444"}]}, "type": "line", "options": {"scales": {"x": {"grid": {"color": "rgba(0,0,0,0.05)"}, "title": {"text": "x", "display": true}}, "y": {"grid": {"color": "rgba(0,0,0,0.08)"}, "ticks": {"stepSize": 2}, "title": {"text": "y", "display": true}, "beginAtZero": true}}}}
   MC[0] expect=2 pattern=sign_error
        msg=The line goes down, so the gradient is negative. Reading the steepness as 2 and dropping the minus reverses the line's direction; it is −2.
   step[0] SAY: Read two clear points, then use rise over run, keeping the sign.
   step[1] pre='At x = 0 the line is at y = ' post='' ANS=10
        hint=Where it crosses the y-axis.
   step[2] pre='At x = 1 the line is at y = ' post='' ANS=8
        hint=Trace up from x = 1 to the line.
   step[3] pre='Rise = 8 − 10 = ' post='' ANS=-2
        hint=Second y minus first y; it falls, so it is negative.
   step[4] pre='Run = 1 − 0 = ' post='' ANS=1 PHASE=substitute
        hint=Difference in the x values.
   step[5] pre='Gradient = rise ÷ run = (−2) ÷ 1 = ' post='' ANS=-2 PHASE=substitute
        hint=Divide the rise by the run.
        done=The line drops 2 for each step across, so the gradient is −2.
======================================================================
TIER SILVER   desc: Handle negatives and fractions: read gradients from points, rearrange equations, and find m or c.
------------------------------------------------------------
[silver][0] input_type=single_value calc=False
 display: Find the gradient of the line through \((−1, 4)\) and \((3, −8)\).
 solutions: [-3]
 answer: null
 hint: The line falls steeply; use rise over run and keep the sign.
   MC[0] expect=3 pattern=sign_error
        msg=Rise = (−8) − 4 = −12, Run = 3 − (−1) = 4, so gradient = −12 ÷ 4 = −3. Dropping the minus gives 3, but the line slopes down.
   step[0] SAY: Gradient means rise over run: how far up for how far across. Start with the rise, the change in y.
   step[1] pre='Rise = (−8) − 4 = ' post='' ANS=-12
        hint=Take the first y from the second y.
   step[2] pre='Run = 3 − (−1) = ' post='' ANS=4
        hint=Take the first x from the second x. Subtracting a negative adds.
   step[3] pre='Gradient = rise ÷ run = (−12) ÷ 4 = ' post='' ANS=-3 PHASE=substitute
        hint=Divide the rise by the run.
   step[4] pre='Check: gradient × run = (−3) × 4 = ' post='' ANS=-12 PHASE=substitute
        hint=Multiply back. It should return the rise.
        done=It gives the rise back, so the gradient is right.
------------------------------------------------------------
[silver][1] input_type=single_value calc=False
 display: The line \(y = mx + 3\) passes through \((2, 11)\). Find \(m\).
 solutions: [4]
 answer: null
 hint: Substitute the point, take 3 off both sides, then divide by 2.
   MC[0] expect=8 pattern=forgot_to_divide
        msg=11 = 2m + 3, so 2m = 8 and m = 4. Writing 8 stops before dividing by 2.
   step[0] SAY: Put the point into y = mx + c and peel away the known parts.
   step[1] pre='Take the intercept off both sides: 11 − 3 = ' post='' ANS=8
        hint=Subtract c from y.
   step[2] pre='So 2 × m = 8. Divide by 2: m = ' post='' ANS=4 PHASE=substitute
        hint=Divide by the number in front of m.
   step[3] pre='Check: 4 × 2 + 3 = ' post='' ANS=11 PHASE=substitute
        hint=Put m back and confirm the point.
        done=It returns the point's y, so m is right.
------------------------------------------------------------
[silver][2] input_type=single_value calc=False
 display: A line has equation \(2y = 6x + 10\). What is the gradient?
 solutions: [3]
 answer: null
 hint: Divide every term by 2 to reach y = mx + c first.
   MC[0] expect=6 pattern=forgot_step
        msg=Divide every term by 2 first: y = 3x + 5. The gradient is 3, not 6.
   step[0] SAY: Get the equation into y = mx + c first by dividing every term by 2.
   step[1] pre='Divide the x term by 2: 6 ÷ 2 = ' post='x' ANS=3
        hint=Divide the number in front of x.
   step[2] pre='Divide the constant by 2: 10 ÷ 2 = ' post='' ANS=5
        hint=Divide the lone number too.
   step[3] pre='Now y = 3x + 5. The gradient is the number in front of x: ' post='' ANS=3 PHASE=substitute
        hint=Read the coefficient of x.
   step[4] pre='Check: from x = 0 (y = 5) to x = 1 (y = 8), the rise is 8 − 5 = ' post='' ANS=3 PHASE=substitute
        hint=One step across raises y by the gradient.
        done=y climbs by the gradient each step, confirming it.
------------------------------------------------------------
[silver][3] input_type=single_value calc=False
 display: Find the gradient of the line through \((−2, −3)\) and \((4, 9)\).
 solutions: [2]
 answer: null
 hint: Both coordinates change sign; subtracting a negative adds.
   MC[0] expect=6 pattern=negative_run_slip
        msg=Run = 4 − (−2) = 6, not 4 − 2 = 2. Subtracting a negative adds. Rise = 12, so gradient = 12 ÷ 6 = 2.
   step[0] SAY: Gradient means rise over run: how far up for how far across. Start with the rise, the change in y.
   step[1] pre='Rise = 9 − (−3) = ' post='' ANS=12
        hint=Take the first y from the second y.
   step[2] pre='Run = 4 − (−2) = ' post='' ANS=6
        hint=Take the first x from the second x. Subtracting a negative adds.
   step[3] pre='Gradient = rise ÷ run = 12 ÷ 6 = ' post='' ANS=2 PHASE=substitute
        hint=Divide the rise by the run.
   step[4] pre='Check: gradient × run = 2 × 6 = ' post='' ANS=12 PHASE=substitute
        hint=Multiply back. It should return the rise.
        done=It gives the rise back, so the gradient is right.
------------------------------------------------------------
[silver][4] input_type=single_value calc=False
 display: A line passes through \((0, −2)\) and \((4, 18)\). What is the equation of the line?  Give the value of \(m\).
 solutions: [5]
 answer: null
 hint: Rise over run between the two points gives m.
   MC[0] expect=20 pattern=forgot_to_divide
        msg=Rise = 18 − (−2) = 20, Run = 4 − 0 = 4. Gradient = 20 ÷ 4 = 5. Stopping at the rise gives 20.
   step[0] SAY: Gradient means rise over run: how far up for how far across. Start with the rise, the change in y.
   step[1] pre='Rise = 18 − (−2) = ' post='' ANS=20
        hint=Take the first y from the second y.
   step[2] pre='Run = 4 − 0 = ' post='' ANS=4
        hint=Take the first x from the second x. Subtracting a negative adds.
   step[3] pre='Gradient = rise ÷ run = 20 ÷ 4 = ' post='' ANS=5 PHASE=substitute
        hint=Divide the rise by the run.
   step[4] pre='Check: gradient × run = 5 × 4 = ' post='' ANS=20 PHASE=substitute
        hint=Multiply back. It should return the rise.
        done=It gives the rise back, so the gradient is right.
------------------------------------------------------------
[silver][5] input_type=single_value calc=False
 display: A line has equation \(3y - 9x = 18\). What is the y-intercept?
 solutions: [6]
 answer: null
 hint: Rearrange to y = mx + c, then read the number on its own.
   MC[0] expect=18 pattern=forgot_step
        msg=Rearrange: 3y = 9x + 18, then divide every term by 3: y = 3x + 6. The y-intercept is 6, not 18.
   step[0] SAY: Rearrange into y = mx + c, then read the intercept c.
   step[1] pre='Move −9x across: 3y = 9x + 18. Divide the x term by 3: 9 ÷ 3 = ' post='x' ANS=3
        hint=Divide the number in front of x.
   step[2] pre='Divide the constant by 3: 18 ÷ 3 = ' post='' ANS=6
        hint=Divide the lone number too.
   step[3] pre='Now y = 3x + 6. The y-intercept is the number on its own: ' post='' ANS=6 PHASE=substitute
        hint=Read the term with no x.
   step[4] pre='Check at x = 0: y = 3 × 0 + 6 = ' post='' ANS=6 PHASE=substitute
        hint=At x = 0 only c is left.
        done=At x = 0 the line sits at c, the y-intercept.
------------------------------------------------------------
[silver][6] input_type=single_value calc=False
 display: Two points on a line are \((1, 7)\) and \((3, 3)\). Find the y-intercept.
 solutions: [9]
 answer: null
 hint: Find the gradient first, then substitute a point to find c.
   MC[0] expect=-2 pattern=read_gradient_not_intercept
        msg=The gradient is −2, but the question asks for the y-intercept. Substituting (1, 7) into y = −2x + c gives c = 9.
   step[0] SAY: Find the gradient, then feed a point back in to reach c.
   step[1] pre='Rise = 3 − 7 = ' post='' ANS=-4
        hint=Second y minus first y.
   step[2] pre='Run = 3 − 1 = ' post='' ANS=2
        hint=Second x minus first x.
   step[3] pre='Gradient m = (−4) ÷ 2 = ' post='' ANS=-2
        hint=Divide rise by run.
   step[4] pre='Use (1, 7): the mx part is (−2) × 1 = ' post='' ANS=-2 PHASE=substitute
        hint=Multiply the gradient by that x.
   step[5] pre='So 7 = (−2) + c, giving c = 7 − (−2) = ' post='' ANS=9 PHASE=substitute
        hint=Take the mx part off the y-value.
        done=That c is where the line meets the y-axis.
======================================================================
TIER GOLD   desc: Work with negative coordinates, rearrange harder equations, and use gradients for c, parallel lines and midpoints.
------------------------------------------------------------
[gold][0] input_type=single_value calc=False
 display: A line passes through \((−3, 11)\) and \((5, −5)\). Find the gradient.
 solutions: [-2]
 answer: null
 hint: The line falls; use rise over run and keep the sign.
   MC[0] expect=2 pattern=sign_error
        msg=Rise = (−5) − 11 = −16, Run = 5 − (−3) = 8, so gradient = −16 ÷ 8 = −2. Dropping the minus gives 2, but the line slopes down.
   step[0] SAY: Gradient means rise over run: how far up for how far across. Start with the rise, the change in y.
   step[1] pre='Rise = (−5) − 11 = ' post='' ANS=-16
        hint=Take the first y from the second y.
   step[2] pre='Run = 5 − (−3) = ' post='' ANS=8
        hint=Take the first x from the second x. Subtracting a negative adds.
   step[3] pre='Gradient = rise ÷ run = (−16) ÷ 8 = ' post='' ANS=-2 PHASE=substitute
        hint=Divide the rise by the run.
   step[4] pre='Check: gradient × run = (−2) × 8 = ' post='' ANS=-16 PHASE=substitute
        hint=Multiply back. It should return the rise.
        done=It gives the rise back, so the gradient is right.
------------------------------------------------------------
[gold][1] input_type=single_value calc=False
 display: The line \(y = mx + c\) passes through \((2, 5)\) and \((6, 17)\). Find the value of \(c\).
 solutions: [-1]
 answer: null
 hint: Find the gradient first, then substitute one point to find c.
   MC[0] expect=11 pattern=sign_error
        msg=From 5 = 3 × 2 + c: c = 5 − 6 = −1. Adding instead (5 + 6) gives 11.
   step[0] SAY: First find the gradient, then use a point to pin down c.
   step[1] pre='Rise = 17 − 5 = ' post='' ANS=12
        hint=Second y minus first y.
   step[2] pre='Run = 6 − 2 = ' post='' ANS=4
        hint=Second x minus first x.
   step[3] pre='Gradient m = 12 ÷ 4 = ' post='' ANS=3
        hint=Divide rise by run.
   step[4] pre='Now use (2, 5): the mx part is 3 × 2 = ' post='' ANS=6 PHASE=substitute
        hint=Multiply the gradient by that x.
   step[5] pre='So 5 = 6 + c, giving c = 5 − 6 = ' post='' ANS=-1 PHASE=substitute
        hint=Take the mx part off the y-value.
        done=That c completes y = mx + c.
------------------------------------------------------------
[gold][2] input_type=single_value calc=False
 display: Line A: \(y = 2x + 1\). Line B passes through \((0, 7)\) and \((2, 3)\). Are the lines parallel? Enter 1 for Yes, 0 for No.
 solutions: [0]
 answer: null
 hint: Find each line's gradient; parallel lines have equal gradients.
   MC[0] expect=1 pattern=sign_error
        msg=Line B gradient = (3 − 7) ÷ (2 − 0) = −4 ÷ 2 = −2, while Line A is 2. They differ, so the lines are not parallel; the answer is 0. Getting 1 usually means B's minus sign was dropped.
   step[0] SAY: Parallel lines have equal gradients. Find each gradient and compare.
   step[1] pre='Line A is y = 2x + 1, so its gradient is ' post='' ANS=2
        hint=The number in front of x.
   step[2] pre='Line B rise = 3 − 7 = ' post='' ANS=-4
        hint=Second y minus first y.
   step[3] pre='Line B run = 2 − 0 = ' post='' ANS=2
        hint=Second x minus first x.
   step[4] pre='Line B gradient = (−4) ÷ 2 = ' post='' ANS=-2 PHASE=substitute
        hint=Divide rise by run.
   step[5] pre='The gradients are 2 and −2. Equal? Enter 1 for Yes, 0 for No: ' post='' ANS=0 PHASE=substitute
        hint=They are different, so not parallel.
        done=Different gradients means the lines are not parallel.
------------------------------------------------------------
[gold][3] input_type=single_value calc=False
 display: A line has equation \(5x + 2y = 20\). Find the gradient.
 solutions: [-2.5]
 answer: null
 hint: Rearrange to y = mx + c, then read the number in front of x.
   MC[0] expect=-5 pattern=forgot_step
        msg=Move 5x across: 2y = −5x + 20. You must still divide every term by 2: y = −2.5x + 10, so the gradient is −2.5. Reading −5 skips the division.
   step[0] SAY: Rearrange into y = mx + c, then read the gradient.
   step[1] pre='Move 5x to the other side. The x term becomes −5x, coefficient ' post='' ANS=-5
        hint=Take 5x off both sides; the sign flips.
   step[2] pre='Divide every term by 2. The x coefficient: (−5) ÷ 2 = ' post='' ANS=-2.5 PHASE=substitute
        hint=Halve the coefficient.
   step[3] pre='So y = −2.5x + 10. The gradient is the number in front of x: ' post='' ANS=-2.5 PHASE=substitute
        hint=Read the coefficient of x.
        done=The rearranged form gives gradient −2.5.
------------------------------------------------------------
[gold][4] input_type=single_value calc=False
 display: The midpoint of \((a, 3)\) and \((7, 11)\) is \((5, 7)\). Find \(a\).
 solutions: [3]
 answer: null
 hint: The midpoint x is the average of the two x-values.
   MC[0] expect=-2 pattern=midpoint_subtract
        msg=Midpoint x = (a + 7) ÷ 2 = 5, so a + 7 = 10 and a = 3. Doing 5 − 7 = −2 skips doubling the midpoint.
   step[0] SAY: The midpoint x is the average of the two x-values. Work backwards to find a.
   step[1] pre='The midpoint x is 5, and averaging halves, so double it back: 2 × 5 = ' post='' ANS=10
        hint=Undo the divide-by-2.
   step[2] pre='That equals a + 7, so a = 10 − 7 = ' post='' ANS=3 PHASE=substitute
        hint=Take 7 off both sides.
   step[3] pre='Check the midpoint x: (3 + 7) ÷ 2 = ' post='' ANS=5 PHASE=substitute
        hint=Average the two x-values.
        done=The x-values average to 5, so a = 3.
